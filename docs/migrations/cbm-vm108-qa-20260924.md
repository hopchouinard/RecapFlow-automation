# VM108 development QA, September 24 2026

Patrick changed the execution order: finish and evaluate the complete Community Brain
replacement on `community-brain-dev` (PVE1 VM108) before preparing one final
production handoff to `home.servers`. The legacy `n8n-automation` VM remains a
recovery-only copy; VM109 and VM101 are unchanged by this development increment.
The older request-by-request production validation sequence is superseded.

## Development system

The development project is `cbm-live-development`. Its PostgreSQL, NATS, files,
configuration and corpus volumes are retained. The API and browser UI use the
immutable application image `sha256:01f5e0099bfda0e3aa2b45cb2e03e00e61f2f344f8568223e6df037edb0946b5`
built from Forge commit `f746dc9`; Open WebUI uses
`sha256:08046b9748558bc2747dd20c9c77fc0e6b05216b8ad33513e7a9152cea15b87b`.
The Forge QA overlay and scripts live at `deploy/community-brain/compose.qa.yml` and
`deploy/community-brain/qa/`; the private VM copy is under
`/srv/dev-data/workspaces/cbm-dev-qa-20260924-v2`. API and WebUI bind only to guest
loopback ports 8090 and 8080. No public route or production publication is enabled.

The WebUI has a fresh persistent data volume, a local QA administrator, signup
closed after setup, the current Community Brain retrieval filter installed and
global, and a real retrieval call to the development API. Its dedicated
`Community Brain (Development)` model is visible and the default; it uses
`gpt-oss:20b` with the exact `docs/inference-guidelines.md` system prompt
(SHA-256 `5d3efe6493e42f8c4322a7291d49ebcf374060040efdcbb72472ebb7d643db2c`).
The model and default survived a WebUI recreation, and an installed-filter
inlet probe emitted transcript context for a development query. The
service-token hashes are scoped to `community-brain-dev`. The API
shows `automatic_processing=true` with a current readiness projection.

The host runner selects exactly one opted-in new-meeting stage per tick. A
persistent selection and start marker prevent automatic replay after a crash or
uncertain result. Acquisition receives only the Fathom credential; processing and
indexing receive only the capped development OpenRouter key. The API, WebUI and
scan container receive neither. Network publication is disabled. Indexing
journals external model requests. After a complete indexed job, the runner
pairs a PostgreSQL dump with the files, configuration, corpus and paused NATS
volumes, verifies the archive, and publishes the development checkpoint state.
Systemd starts the stack on boot and invokes the runner every 30 seconds after
the previous tick ends.

Before switching the API, Forge retained a root-private PostgreSQL and volume
backup under `.../backup-pre-qa`; its manifest SHA-256 is
`7017a9a9022730c0e8f7d59fbec39aa2e0572b844ca8c0452aa11c15a9110042`.
The unchanged prior API image and backup are the rollback basis. Development
service identities were rotated locally on VM108, with old QA tokens revoked
and new ones expiring **October 24 2026, 04:18:13 UTC**. The repeatable
`qa/rotate_vm.py` command uses a new dated generation label for the next
development-only renewal; it does not change Authentik or production identities.
The test key has a US$5 total allowance with no reset; the worker checks
remaining allowance before every paid request. After this synthetic run the
remaining allowance was **US$3.9133**, so QA should use the existing completed
meeting first and reserve new full loops for the cases Patrick wants to evaluate.

## Patrick's connection

On the Mac that has the existing VM108 administration SSH identity, keep this
terminal open:

```sh
ssh -N -L 127.0.0.1:8090:127.0.0.1:8090 -L 127.0.0.1:8080:127.0.0.1:8080 pchouinard@10.1.30.20
```

Open `http://localhost:8090` for the Community Brain application and sign in
through the existing Authentik account. Its callback is registered to this exact
origin. Open `http://localhost:8080` for the development Open WebUI; select
`Community Brain (Development)` in the model picker if it is not already
selected. Its local QA
account is `community-brain-qa@example.invalid`; retrieve its generated password
privately on the Mac with:

```sh
ssh pchouinard@10.1.30.20 'sudo -n cat /srv/dev-data/workspaces/cbm-dev-qa-20260924-v2/private/admin.json'
```

That command prints the private QA account record to the Mac terminal. Do not
paste the password into the repository or a shared handoff. The first browser
QA should verify login, a new weekly meeting with both transcript and chat,
visible processing artifacts, indexed retrieval, and a WebUI conversation using
the Community Brain filter. Real Fathom acquisition can be tested with a scoped
recording URL or ID in the dev application; its key never enters the model worker.

## Development acceptance and remaining production work

The VM108 synthetic weekly job `be959a3b-fa8a-460f-896c-2eb2a52eeac4`
completed ten processing model calls, six artifacts, twelve journaled indexing
model calls, a complete search index and a verified paired checkpoint. The
checkpoint manifest SHA-256 is
`fc3636af3bc6ec55e2fc76de3328271d9008562f5696485a7f5dd37dc8e10b17`.
The job API reports processing `succeeded`, indexing `complete` and backup
`verified`; a scoped retrieval returns ten chunks for the new session.
Open WebUI returned HTTP 200 and an answer through a local Ollama model with
its global filter active; signup is still closed after API recreation and token
rotation. The API reports `ready` and the runner timer returns to `idle`.
The first short chat through the dedicated model returned an answer without a
source citation. The installed filter independently emitted retrieved transcript
context, so citation behavior remains a specific item for Patrick's browser QA.
The checkpoint verifies dump readability and archive integrity; a separate
restore exercise remains part of production handoff validation.

The separately selected Fathom acquisition job
`238d57f3-b04b-4d11-864a-7d7baa4ab29c` succeeded through the acquisition
worker. Its private transcript source is 86,144 bytes and has SHA-256
`76c44ab52cd4105d6c4d9fd0762c5d4a6e72f9ffc10721175f4969368a918cd5`,
matching the [earlier VM108 receipt](cbm-fathom-fixed-dev-receipt.json). No
chat was supplied, so processing remains `waiting_for_input`; the job has zero
model calls. The OpenRouter allowance stayed at US$3.9133. This tests real
Fathom acquisition without sending that real transcript for model processing.
Both QA test jobs remain visible in the development application.

Patrick's browser QA is the remaining development acceptance step before this
becomes a candidate for production. Record his findings against the exact
source/image/configuration manifest and the rollback evidence. Only after his acceptance should
Forge prepare one consolidated `home.servers` handoff for protected production
data/signing/configuration restoration, final environment validation, migration
of Open WebUI and its consumers to VM109, controlled cutover and rollback. That
handoff should not ask for intermittent partial production checks. VM101 remains
recovery-only until separate retirement evidence closes.
