# CBM-PREVIEW-20260910-012

Patrick confirmed the automatic form and explicitly requested Raw/Rendered
Markdown toggles plus all years/months collapsed by default. This authorizes this
frontend-only deployment under the existing scope. No backend, auth, worker,
corpus, publication or spending change; no test submission or model call.

Prepared packet: VM109 /srv/community-brain/workspaces/cbm-preview-20260910/.
frontend-manifest.json (companion cbm-preview-manifest.json) pins the3 dist files.
Use the current Infisical-backed automatic runtime renderer/overlay. Bind the new
static directory read-only, merge/retain existing hashed assets without collisions,
update effective frontend/runtime/renderer pins and recovery-control inventory,
then recreate API only as necessary with current authority. Preserve the original
source packet and all automatic guards/schedulers. No stale api.env restoration.
Back up the reviewed old/new controls privately and off-host, preserving prior pairs.

Raw is the default. Both archive and Recent runs use the same FilePreview component.
Markdown files (.md/.markdown/.md.legacy) have accessible Raw/Rendered buttons above
the content. Text files stay literal. Rendering uses locked npm react-markdown and
remark-gfm versions; built locally into static assets, no runtime CDN/dependency
install. Raw HTML remains disabled, unsafe URLs filtered and external image loads
replaced by text labels. Heading/list/table/code/blockquote styling follows existing
theme tokens, with a scrollable preview and mobile controls. Copy/download always
use the original source. Each newly opened file starts raw.

All years/months start collapsed, including the latest. Date search intentionally
opens matches; clearing search collapses all again. Recent runs remains available.
Six frontend unit/component tests, three Playwright scenarios and build/typecheck
passed. Browser checks cover collapsed latest/old years, keyboard expansion,
search reset, rendered headings, original download bytes while rendered, raw
roundtrip, dark/mobile layout and unchanged automatic submission behavior.

Verify trusted HTTPS root/current+retained assets, auth configuration, API automatic
flag and idle runner, catalog87/481 and1901-row FTS. Preserve all existing DB and
managed-data fingerprints. No synthetic production job is needed. Return a concise
completion receipt with effective pins, deployment checks and private backup paths.
If Forge's verification is needed, say exactly which check is pending; don't wait
for an unstated approval. Patrick already requested this deployment.

Request011's live activation has been independently accepted by Forge and Patrick
confirmed the form. Its final management receipt remains outstanding. Close that
administrative evidence separately; do not replay its deployment or workers. This
UI packet is ready as an explicitly authorized follow-up, not a new migration phase.
