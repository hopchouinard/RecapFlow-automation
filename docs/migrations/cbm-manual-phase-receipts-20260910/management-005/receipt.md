# CBM-MANUAL-ACTIVATE-20260910-005 completed

All six request document hashes and all14 installed runtime asset hashes matched. The installed runtime-manifest.json is the files mapping, while the attached public manifest wraps that mapping with archive_sha256; the mappings match exactly.

The specifically approved manual API restart passed15 TLS/auth checks. Infisical live authority was freshly merged from3 to5 identities, preserving every existing identity and secret value. The collector has only sources:upload:chat; transcript/aliases uploads and job access are403. Missing/invalid clients are401; the manual operator can read jobs but cannot read metrics. Existing read, metrics and retrieval identities work. API loads13 preserved cue rules; files is writable, corpus/config remain read-only; no provider/queue credentials or enabled model/publication flags enter API. API and Alloy are the only running production containers.

The actual cached Open WebUI filter passed against the unchanged active production URL, emitted transcript context and returned10 sources. Its content hash is unchanged. All13 corpus and13 configuration file hashes match the accepted checkpoint;1901 successful rows,87 sessions and full1901-row FTS coverage are unchanged.

Existing Fathom authority remains Infisical homelab/development /applications/community-brain, key CB_FATHOM_API_KEY. Acquisition-only delivery is /etc/community-brain-production/acquisition.env, root0600 under root0700, with that single entry. No rotation, fresh n8n extraction, provider request or other client delivery occurred. It is for selected call812746883 / recording181075701 only.

Production Mac manual collector and exact private selection are active at the paths in collector-activation.json. One selected chat upload succeeded: source dcba207e-92be-4661-995f-c48194a8f9af,6804 bytes,SHA256 5300e1d44b776254042f955493de9a88545e64b10a05cf014d0123a4224d6084. The one authorized deduplication request returned the identical receipt. Database counts after both requests: sources4,jobs2,ModelCalls14,unsent outbox2. No job submission or model processing by management. The initial receipt verifier expected two fields omitted by the API; the exact persisted chat row was reconciled read-only before deduplication, with no uncertain upload retry. The first Mac Folder Action lookup transiently failed before any activation/upload; subsequent enumeration and exact lookup verified the existing association disabled.

Private API rollback is preserved at /etc/community-brain-production/manual-activation-005/, including api.env.before, compose.before.yml, the original private container inspection and identities.before.json. Concrete command: sudo python3 /etc/community-brain-production/manual-activation-005/rollback-api.py. It restores only saved API configuration and base Compose, never legacy writers. If used, verify health and actual WebUI retrieval and reconcile only the two activated identities in Infisical against the saved map; preserve unrelated later authority changes. No API rollback was needed.

Legacy n8n stopped, old retrieval paused, both restart policies no;2867 immutable inodes and saved cron hold remain verified. Mac Zoom Folder Action stays disabled and old LaunchAgent unloaded. Independent rollback still starts September11 01:25UTC, harddeadline01:30UTC. Probe renewal remains due before02:05:38UTC. No timer cancellation/extension, corpus/session replacement, worker start, legacy hold release, remote publication or retirement is granted by this receipt. Forge may continue its separately approved selected acquisition/processing; the existing September8 indexed session remains protected.

Public machine receipts accompany this file. Secret values and source content are excluded.
