# CBM-MANUAL-RECOVERY-20260910-006 completed

Management verified all10 request document hashes, claimed once, and completed recovery, explicit probe renewal and the ownership proposal. Deadline cancellation and ownership supersession were NOT executed.

The fresh logical dump is platform-db:/var/backups/community-brain/20260910T061730Z.dump,56658 bytes,SHA256 3937db5e4f88b3c508bbbac5feac84f0443071b0d7d5709fa490397b53d973cd. All10 public tables, exact rows, schema/owners/grants matched after isolated restore; runtime SELECT/INSERT/UPDATE/DELETE grants were individually verified on all9 application tables. Production state remained unchanged across dump, restore and final management checks: jobs3,sources6,artifacts15,ModelCalls29,stages15,outbox7,three inert unsent indexing events. The disposable database used connection limit0 and revoked PUBLIC CONNECT; no worker, queue or provider access occurred.

Both archives restored into private isolated copies:79 managed files,42 runtime/approval files, all50 database-to-file references. The copied corpus passed a network-disabled read-only check:1901 successful rows,87 sessions,768 dimensions,FTS1901indexed/0unindexed. Temporary database, extracted directories and verification container were removed. Live file manifests matched before/after pairing; current database hashes still match after renewal. This establishes a quiescent logical pair, not simultaneous cross-host snapshots, PITR or a full guest restore. Later API recreations changed operational logs/cache after the archived runtime capture; executable source packets and the paired archives remain preserved.

The paired dump is also on VM109:/srv/community-brain/db-backups/20260910T061730Z.dump. Both archives, their manifests, the dump and paired-recovery-manifest.json were copied and hash-verified off-host under platform-db:/var/backups/community-brain/manual-20260910-006/. The matching paired manifest is in VM109:/srv/community-brain/artifacts/cbm-manual-production-20260910/. The associated PBS snapshot pbs:backup/vm/109/2026-09-10T06:28:34Z completed with both guest disks and guest-agent freeze/thaw. Prior snapshots and paired checkpoints were retained; no pruning requested.

Only read/metrics probes were rotated through Infisical. New expiry:2026-09-17T06:22:56Z,seven-day lifetime. Old/new acceptance overlap was verified before switching Kuma31 and the existing Prometheus token file, then the old hashes were removed. Old tokens now401; cross-scope requests403; final identity count5. Human/retrieval/collector/operator/Fathom/model identity values and privileges stayed unchanged. No spending limit change or provider call. The first Kuma attempt stopped before mutation; read-only reconciliation confirmed its original header and absent pre-edit backup, then one live update succeeded. All other monitor settings were preserved.

The effective recreation helper is installed in the existing Mac renderer as manual_api_runtime.py. It freshly renders only the API bundle and verifies14 assets before invoking the approved manual_host.py api-up with BOTH Compose files. Two controlled API recreations passed. Fifteen subsequent TLS/auth checks,13 cue rules, correct read-only corpus/config and writable files mounts, no provider/queue keys in API, and only API/Alloy running were verified. The actual cached Open WebUI filter still uses the accepted URL/key and unchanged source, returns10 sources and emits transcript context. Kuma30/31 and both Prometheus targets are healthy with samples after final rotation; hourly Mac maintenance passed at06:26:01UTC.

OPERATIONS.md documents the current recipe, private paths, expiry ownership and recovery limits. ownership-supersession-proposal.md gives the concrete follow-up procedure, including terminal fail-closed state and coordinated safeguards for BOTH the VM101 rollback/boot/manual controller and Mac resume hook. Current legacy services/restart policies, cron hold and all2867 immutable inodes remain verified; exact legacy Zoom Folder Action disabled and LaunchAgent unloaded. Independent rollback stays armed for September11 01:25UTC /harddeadline01:30UTC.

Remaining readiness considerations: the hourly Mac copy can miss the nightly PBS run after the00:30UTC DB dump; routine scheduled backups are not automatically quiescent paired sets; identity renewals remain explicit; System Events cold folder-action lookup needs careful handling in any future superseding hook. These are documented for final ownership review, not silently waived. Today's explicit copied/PBS checkpoint is verified. No existing-session replacement, remote publication, general worker, legacy writer release, service/data retirement or final quality closure occurred. Two real weekly cycles remain outstanding.

Companion files are listed with hashes in response-files.json. Secret values, raw content and database dumps are excluded from the relay. Continue with a new request after acknowledging this one; no repeated recovery or credential rotation is needed.
