# CBM-SIGNAL-20260910-014 — approved theme rollout

Patrick reviewed the dark/light screenshots and explicitly instructed:
"Yes I approve. Roll it out". Proceed with this frontend-only deployment.

Immutable verified packet on VM109:
`/srv/community-brain/workspaces/cbm-signal-20260910/`
Companion cbm-signal-manifest.json pins dist and frontend-manifest.json uses the
same schema. Original packet also verified on VM108 under /srv/dev-data/workspaces.

Use the current authoritative Infisical-backed runtime renderer and ordered quiet
locks plus Mac scheduler lock, following the completed request012 static rollout.
Preserve original packet; merge all existing hashed assets into a separate effective
static directory without overwriting collisions. Update effective frontend/runtime/
Mac renderer pins and recovery inventory. Recreate API only as needed using fresh
identities; never restore stale api.env. Preserve backend source/image, auth,
automatic runtime, boot/quiet/recovery guards, cron and all existing data/config.
Capture old/new deployment controls privately and off-host, retaining prior pairs.

Change: Patchoutech Signal CSS derived from request013 reference — cyan/amber,
blue-black atmospheric background, glass panels, thin grid/trace details, matching
light theme, explicit Raw/Rendered selected state. Appearance preference retained.
No new font download, dependency, external asset call or JS behavior change. JS
bytes exactly match request012 although Vite generated a new asset filename.
No new submission or model call, provider spend change, publication, historical
replacement, retirement or migration phase activation is authorized here.

Validation already passed: build/typecheck, six frontend unit/component tests,
three browser scenarios, plus expanded desktop/mobile dark/light screenshots,
no mobile overflow, date tree collapse/search, raw/rendered original download,
and existing automatic submission behavior. Patrick accepted these screenshots.

Verify trusted HTTPS root and current/retained assets, API/auth behavior,
automatic_processing=true, idle/healthy runner, 87 meetings/481 archive files,
1901 fully FTS-indexed rows. Compare DB and managed-file fingerprints before/after.
Do not submit a synthetic job to test styling. Return final receipt, effective pins,
checks and private backup paths with hashed safe evidence. Say explicitly if an
independent Forge check is needed; no new approval from Forge/Patrick is pending.

Request013 delivery is verified and acknowledged. Requests011/012 activation and
controls are complete per012 receipt; do not replay them. All publication,
replacement, retirement and broader migration gates remain in force.
