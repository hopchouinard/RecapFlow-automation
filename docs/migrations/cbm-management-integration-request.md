# Request027 — development management integration and coherent successor renderer

Request ID: `CBM-MANAGEMENT-INTEGRATION-20260920-027`. Target: home.servers.
Request026 is verified and acknowledged. Patrick explicitly authorized continuing
VM108 integration and recovery tests. n8n-automation remains recovery-only.

This request is **development-only implementation/validation and read-only rollout
preparation**. No production rotation, source installation, data transfer, DNS or
route change, cutover, restart, boot reconciliation, hold clearance, legacy backup
credential update, processing resume or retirement is authorized here.

## Inputs and division of work

Forge's integration harness runs the actual stabilized Community Brain API,
PostgreSQL, LanceDB/FTS and exact WebUI/filter on an isolated VM108 network with
synthetic data. Query embeddings/model-list are fixtures. The unchanged Request024
five-identity policy exercises real API recreation and WebUI delivery/cache checks,
but its authority and other consumer bundles are private development fixtures.
That is not certification of Infisical, actual management transport/leases,
Kuma/Prometheus/collector adapters or the production renderer.

The attached exact source/receipt manifest identifies the validated Forge input.
Real retrieval, five-identity policy recovery, fresh-volume/Chroma restore and
concurrent pipeline/load checks passed; the fixture is now stopped and retained.
Do not replay its setup/mutation phases or alter existing private fixture state.
Keep that workspace available for inspection; use a separate dedicated management
integration workspace after checking VM108 capacity and current activity. Preserve
all pre-existing development services and the stopped Request026 fixture.

## Required work

1. Reconcile the exact current management code from Request024 and its source
   ownership. Implement an explicit replacement WebUI target and successor runtime
   descriptor. Production must not require SSH, delivery or serving from VM101.
   Retain journal/overlap/revocation semantics and the terminal superseded intake
   guard. Produce source-controlled candidate changes without installing them live.
2. Using existing management administration, validate the actual Infisical
   read/save/verify path with isolated development authority and generated
   development identities only. Do not reuse the production project/path/journal
   or tokens. If an appropriately scoped development authority cannot be provided
   within existing authorization, report that specific blocker; do not substitute
   a file/mock authority and label it an Infisical pass. Keep all values private.
3. Exercise actual candidate management transport and consumer adapters against
   dedicated development targets, including the replacement WebUI/API, scoped
   development collector/probe bundles and isolated monitoring consumers. Validate
   scheduler mutex/quiet lease, lost acknowledgment, interrupted delivery/resume,
   old/new acceptance, fresh consumer verification, then old rejection. Describe
   any adapter boundary that remains simulated. Do not touch installed production
   monitors/collector clients or rotate production credentials.
4. Implement a coherent immutable successor renderer/host packet. Bind the tested
   application image
   `sha256:6e7f43ebd7970f89ae9f1afe5d4d77b89448e188e4a580ff9e38bac923d5bc5b`,
   verified WebUI image relationship from Request026, exact helper/module/frontend
   hashes and external signing-material delivery. Reconcile run.py/indexing image
   pins, old module/frontend mounts, manual_api_runtime manifest checks, boot
   launcher, recovery image/file inventory and management controls together. Do not
   modify r020 in place or bypass its assertions. On VM108, prove renewal-driven
   API recreation retains the intended source/image/UI and preserves holds.
5. Return a reviewable production rollout/rollback packet with protected data and
   signing-key transfer requirements, source-control provenance, before-state
   capture, ordered locks, staged verification and recovery. Propose a distinct
   WebUI TLS route preserving current local authentication and explicit signup
   policy; inspect availability/conflicts read-only. Do not apply production DNS,
   ingress, monitoring or allowlist changes. Capture exact remaining decisions and
   development gates instead of treating a proposed configuration as deployed.
6. Recheck only nonsecret production identity expiry/journal metadata. Last known
   expiry is September22 20:53:07 UTC. Aim to return by September21 20:00 UTC;
   expiry never permits bypassing validation or restoring VM101's production role.

Claim once per PROTOCOL.md. Return safe source/diffs, hashes, actual development
results, private-artifact locations/checksums, retained-fixture/cleanup status and
precise remaining gates. Do not export credentials, real chats, provider content
or production databases. Forge will reconcile returned source; no push or live
installation is implied by this request.
