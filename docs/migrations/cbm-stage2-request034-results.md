# Request034 consumed: development qualification, controller still open

September24. Forge verified the eight named response hashes, the safe source
archive, the final VM108 `packet-v12` manifest and every shared source file.
The imported source is retained unchanged at
`deploy/community-brain/stage2-qualification-034/`. Agent-ops source commit:
`6bdd50086646337a6db2b103b1dd28910db33635`, not pushed. Final VM108
manifest: `7c5cfba2187e72ea40a6955592278539f7982376abbe647819e466354b3b3825`.
Forge reran7 admission and9 successor boundary tests successfully. Historical
integration effects were read, not replayed.

Actual development Infisical and fresh Step CA issuance passed with scoped
identities. The API/WebUI filter, collector configuration, Kuma, Prometheus and
development NATS accepted the new generation; loss of delivery acknowledgment
reconciled the same generation before old credentials were revoked. Current,
expired, wrong and cross-scope requests returned200/401/401/403 on the isolated
API. The generated development identities expire September30 22:57:49UTC; the
issued NATS leaf expires September24 22:50:50UTC. These are development receipts,
not production renewal authority.

Exact-image WebUI browser login, retained synthetic chat/upload, disabled signup,
original-key session and wrong-key rejection passed. Paired post-browser recovery
matched an independent PostgreSQL and114 files, including35 vector files; FTS had
17 indexed and zero unindexed rows. Capacity on the4GiB VM passed the declared
75-second profile:677 request pairs, no failures/OOM, p95 516.4ms, peak observed
container CPU52.32%, minimum available memory1,768,036KiB. API, WebUI, worker
and restore overlapped for4.8seconds. This is bounded acceptance, not a long
continuous four-way restore or paid-provider latency proof. Forge's separate
fresh fixture also passed350 pairs and rendered browser checks; see the
[Forge results](cbm-stage2-forge-results.md).

Four failed/interrupted preparation attempts remain retained and are not counted
as successes. Seven Request034 fixture containers were stopped with volumes and
journals retained; the four standing VM108 services continued running. VM109's
original API/Alloy remained running. VM101 stayed recovery-only. No production
renewal, installation, protected transfer, resize or provider spend occurred.

**Stage2 is still open.** `production_admission.py` validates a reviewed plan's
binding and exact21 evidence names but intentionally refuses any production
execution switch. It accepts `accepted:true` evidence envelopes as review input;
that check is not independent proof of the underlying recovery facts. There is
no bounded controller, installer or VM109 independent finalizer, and no final-byte
VM108 fault matrix for them. Both legacy production refusals and all21 null slots
remain. The five production service identities expired September22 20:53:07UTC;
read-only status reported the current renewal policy failing with AssertionError.
A separate renewal/production phase remains required; expiry cannot be waived
by this development work.

Request035 targets exactly the missing controller/installer/finalizer and fault
matrix under the already authorized development-first stage2 scope. It must
produce reviewable source and development acceptance without executing a
production phase or filling evidence slots from dev receipts. Protected recovery,
real user sessions, persistent production enrollment, provider cost acceptance,
serving cutover and processing resume remain later gates.

[Home.servers ledger](receipts/cbm-stage2-qualification-20260923/acceptance-ledger.md),
[verification](receipts/cbm-stage2-qualification-20260923/verification.json),
[Forge source check](receipts/cbm-stage2-qualification-20260923/forge-source-verification.json).
