# Successor validation — September 20, 2026

Packet reproducibility is fixed and tested on Forge and VM108. JSON serialization
now sorts keys recursively; a regression test reverses filesystem traversal and
input mapping order. Seven boundary tests pass on both hosts. Packet-v8 has the
same manifest bytes on both filesystems:
`926423440efd0439e6c1d85c966872c632c13bb3abf0bab833b1c380efffbe1a`.
The original packet-v7 and r020 remain unchanged. Packet-v8 is a build result,
not an installed runtime or production candidate.

The actual imported selected-worker execute adapter passed processing/indexing
for one synthetic meeting and acquisition/processing/indexing for another on
VM108. PostgreSQL, JetStream, durable stages, artifacts and LanceDB were real;
external providers, allowance authority and queue binding were fixtures. All
five stages succeeded, 16 rows were indexed with no FTS gap, the first meeting
was preserved and excluded work/outbox remained untouched. Host launchers and
paid-provider budget enforcement are not certified by this run.

The first setup failed before execution because source permissions excluded
UID10001. A subsequent run completed processing but failed indexing because the
test mocked module-level embedding only, while the adapter supplies a client URL.
The final fixture covers Client.embed too. Another source sync restored restrictive
permissions; they were corrected on the source-only tree before the final passing
run. No failure was counted as acceptance and all state was disposable.

All 40 exercised source files matched Forge. The three disposable containers
stopped without OOM and were removed; the four pre-existing dev services remain.
Safe evidence and hashes are in the
[receipt directory](receipts/cbm-successor-validation-20260920/verification.json).

## r020 disposition

The extra bytecode file has SHA256
`084a3fb857c3499d9198dcd40201e524fa5a4c2282e22e74998bd4f4ab21853f`.
Its modification/change timestamps are September17 02:48:15 UTC, owner root,
mode0644. The timestamp-based header matches current run.py size and modification
time. This is consistent with an earlier import but does not prove its creator
or bytecode equivalence. It predates Request027. Preserve it and the exact-file-set
failure; do not delete it or loosen the old manifest. A successor must be a newly
sealed workspace with explicit recovery provenance, not a repair in place.

## Configuration and worker gaps found in actual source/runtime

- Request027's bounded_worker still hardcodes the production TLS NATS endpoint,
  production stream/subject and inbox prefix. Its dev Docker network alone does
  not constitute an executable development transport configuration. The fixture
  override is explicit and is not a shipping fix for this gap.
- The live production API has separate archive, corpus, files, config, public
  status and CA mounts, including read-only boundaries. Request027 renders a broad
  state mount and omits several of these bindings. Production currently has a
  2 GiB API limit; the dev renderer uses 900 MiB. Resource acceptance must use the
  proposed full configuration, not silently inherit that reduction.
- The live API remains on image be0e7d… with r020 module/frontend overlays. Its
  network is community-brain-production-staging_default; the development workers
  hardcode cbm-r027_default. Paths, volumes, identities, automatic/manual launchers,
  boot controls, signing delivery and recovery inventory need one coherent binding.
- Current launchers may spawn Python without -B. Prevent generated bytecode in
  future immutable packets and test automatic/manual subprocesses against the
  exact-file-set gate. Do not change old recovery state to accommodate it.

Request028 carries these concrete gaps and the validated rendering fix to
home.servers for the Mac/management portion and complete VM108 integration.
Production source installation, private data/signing transfer, rotation, cutover,
processing resume and retirement have not occurred. Previously authorized ingress
still waits for a ready replacement; no repeat ingress approval is needed.
