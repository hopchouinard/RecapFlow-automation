# OpenWebUI integration and recovery — September20

Patrick authorized this continuation. All execution took place on VM108 using
synthetic data and generated development identities. Production and the dormant
legacy recovery VM were not changed.

## Completed integration

The real stabilized API factory, PostgreSQL, LanceDB/FTS and exact OpenWebUI/filter
now work together in an isolated development stack. The filter returned the
expected seeded corpus chunk through the actual authenticated retrieval endpoint.
Wrong credentials and cross-scope reads were rejected. Only query embeddings and
the model list are deterministic HTTP fixtures; no canned retrieval endpoint or
paid inference is involved.

The exact Request024 five-identity renewal policy passed with actual API recreation
and WebUI delivery/live-cache checks. A lost acknowledgment after WebUI delivery
left all10 old/new identities accepted. The same prepared generation resumed,
consumer checks completed, the old5 identities were rejected, and the new key
survived WebUI restart. Three API recreations retained image
`sha256:6e7f43ebd7970f89ae9f1afe5d4d77b89448e188e4a580ff9e38bac923d5bc5b`
without old jobs-module or frontend overlays.

**Boundary:** authority storage and the other consumer bundles are development
fixtures. This is not an Infisical, installed Kuma/Prometheus/collector, production
renderer or management-lease acceptance claim. Those require the management
integration handoff before production promotion.

## Fresh-volume recovery

Synthetic chats, uploaded file bytes, custom models, prompts, authentication and
actual filter behavior passed. A stopped-volume backup was restored into a fresh
volume; all recorded file hashes and internal cache symlinks matched before boot.
The restored application then passed login/data checks, a Chroma vector query and
live retrieval from the actual Community Brain backend. External signing material
was preserved separately. The original fixture volume remained unchanged.

The first manifest check rejected legitimate bundled model-cache symlinks before
copying anything. The checker was corrected to preserve internal links and reject
escaping links. Three focused tests passed on Forge and VM108. Recovery resumed
from the stopped-volume boundary without replaying chat/file/vector creation.
Earlier setup corrections fixed source readability, fresh-volume ownership and
an inherited test-health URL; credentials and completed effects were preserved.

This proves synthetic recovery mechanics, not restoration or quality acceptance
of the60 real chats,111 files and legacy knowledge/vector state. Their protected
transfer and verification remain part of the production packet.

## Capacity and isolation

The receipt records concurrent real retrieval/filter requests alongside the full
synthetic two-meeting pipeline checker. The extended load window covers the whole
pipeline process, avoiding the shorter first run's incomplete overlap. Pipeline
processing/indexing, acquisition, complete FTS and preservation checks passed.
The final run completed316 request pairs across3 concurrent clients, overlapping
the entire two-meeting pipeline process. The28 memory samples observed a minimum
of approximately1,702MiB available; no OOM was recorded.
Small synthetic data and deterministic embeddings do not certify production-scale
load or real-provider latency. Treat measured headroom as bounded development
evidence, not an unconditional production sizing guarantee.

All four integration services use one internal Docker network without published
ports. An external documentation-address route probe returned ENETUNREACH. The
fixture supplies no production endpoint or legacy-host transport. This is source
and network-isolation evidence, not packet-capture certification.

## Source, evidence and remaining work

[The harness](../../deploy/community-brain/openwebui-integration/README.md) uses
existing pinned software and unchanged Request026 helper/filter sources. The
focused recovery tests are included in canonical Forge verification. No runtime
application source or dependency was changed. The full unchanged application/UI
suite was not repeated; the actual VM108 pipeline/stabilization checker was run.

[Safe results and source hashes](receipts/cbm-openwebui-integration-20260920/verification.json)
record validation, workload overlap, cleanup and explicit limits. Private fixture
credentials, backup archives and databases stay on VM108 outside Git/relay files.

Next: home.servers must validate actual development Infisical/consumer/lease
integration and produce the coordinated successor renderer/host packet. It owns
the existing management access and source needed for those checks. Request027
provides the concrete candidate and acceptance evidence; it authorizes development
and read-only preparation only. No production rotation, deployment, user-data
transfer, ingress change, hold clearance or retirement is performed here.
