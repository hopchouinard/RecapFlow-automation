# Request030 — finalize protected restoration preparation

Request ID: CBM-PROTECTED-RESTORE-PREP-20260921-030. Target: home.servers.
Deadline: September21 at20:00 UTC. Patrick asked to proceed with the next task:
prepare the protected real-data/signing restore and acceptance packet. Existing
read-only infrastructure access, private relay and VM108 development administration
apply. This request does not authorize production capture/transfer, data/signing
extraction into a new store, installation, rotation, ingress, cutover or processing.

Request029 is consumed and acknowledged. Read the attached protected restore packet
and latest results; verify the request manifest. Use your existing Mac/OpenWebUI,
Infisical and backup administration access for read-only metadata and development
work. Do not copy production values/content through the relay or restart VM101.

1. Resolve the packet's outstanding exact source, database, authority, stopped
   WebUI/signing, backup, independent restore destination and storage bindings.
   Refresh counts/hashes/identities without exporting content or changing originals.
   Distinguish current VM109 Community Brain data from dormant VM101 WebUI data.
2. Produce exact operator commands and a private manifest schema for a unique,
   paired capture, protected transfer, independent restore and acceptance. Separate
   preservation originals from sanitized dev clones. Bind ownership, contained
   links, endpoint/credential substitution, retention and safe abort/resume rules.
3. Implement any missing preparation/recovery primitives and test on fresh synthetic
   VM108 state only. Existing validation_host.paired/finish_pair have fixture restart
   and checkpoint-clear effects: do not reuse them as production transfer commands.
   New capture/restore verification must preserve holds and make no worker effects.
4. Reconcile the actual receipt schema with the disabled production plan compiler.
   Return the completed preparation artifact and explicit remaining capture/transfer
   approval boundary; do not fake completed slots or enable production compilation.
5. Prepare concrete capacity and real-provider/budget test cases using scoped dev
   identities. Run only tests already covered by development authorization and
   spending limits; otherwise return the exact bounded proposal. No production keys,
   paid replay, live desktop-intake change or real-data transfer under this request.
6. Return source/diff/provenance, VM108 receipts, safe inventory metadata and a
   reviewable next-phase proposal naming source/destination, data scope, commands,
   verification, rollback owner and retention. Identify which browser/session checks
   are synthetic development versus later real staged-deployment verification.

Preserve all Request026–029/Forge fixtures and uncertain outcomes. Recheck expiry
metadata (last September22 at20:53:07 UTC) and report deadline risk. This does not
authorize production rotation, extending a generation or reactivating VM101.
