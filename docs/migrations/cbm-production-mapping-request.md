# Request033: finish actual production mappings

ID: CBM-PRODUCTION-MAPPING-20260922-033. Target: home.servers.
Deadline:2026-09-22T18:00:00Z. Request032 consumed and acknowledged.
Patrick explicitly instructed: **Finish production mappings.**

Authorization: source/inventory reconciliation, read-only production metadata,
implementation in isolated candidate source and validation on VM108. Existing Mac,
DB-host and infrastructure administration access may be used for those purposes.
No production helper invocation, renewal, rotation, install, maintenance stop,
protected capture/transfer, data/signing import, cutover/resume, resize or provider
spend. Refresh expiry metadata read-only; expiry is a stop condition, not authority.

Close the four mappings described in cbm-production-mappings.md. Return a finished
mapping/implementation packet, not another generic synthetic rehearsal or a list
that defers the same mappings again. If a real blocker prevents completion, name
its exact missing input/access/authority and complete independent work first.

1. Fetch/read/hash actual deployed helper sources behind both installed scheduler
   scripts, manual management and their wrappers. Include the nine helpers named
   in Forge's mapping checker, run.sh and all transitive effect-bearing code.
   Inventory real effects, mutation targets, deadlines, uncertain-outcome readback
   and recovery. Implement the candidate adapters against those real contracts,
   substituting scoped dev identities/targets only. Exercise actual helper logic
   in VM108/Mac isolated state, not a replacement health-only helper. Map obsolete
   VM101 consumer references to the successor; never renew/restart VM101 or keep
   it as a functional production dependency. Preserve processing/checkpoint holds.
2. Independently obtain a metadata-only admin production DB census and compare
   to the actual Forge census: public/plpgsql, ten cb_* and Alembic tables,19
   indexes,zero sequences. Scope roles/membership/writers to this application on
   the shared DB host. Do not revoke unrelated roles or terminate production
   clients. Implement the explicit actual profile, including zero sequences and
   actual relationships/unknown outcomes/model_calls/operations/rejected_events,
   and test synthetic data using the real schema on isolated PostgreSQL18.6.
   Close every unsupported catalog finding or explicitly refuse it. No production
   rows/dumps/secrets may enter fixtures/relay. Keep definitions with private
   literals private; only safe schema metadata/source should cross the relay.
3. Bind persistent service templates to actual mount/boot guard/cron/Docker and
   control-directory ownership. Address Docker unless-stopped serving startup,
   shared admission and unchanged boot processing pause. Resolve finalizer/startup
   semantics with the exact incumbent and source versions. Test actual candidate
   units on isolated dev fixtures without rebooting/altering the baseline stack.
   Distinguish tested ordering from actual reboot proof; no unconditional guarantee.
4. Finalize exact filesystem/signing metadata policy, component membership, private
   off-host destination and independent restore contract. Reconcile Request030's
   proposed offline private VM108 enclave versus later restrictions: choose and
   name a suitable private restore destination in the proposal, but do not transfer
   any private bytes. Bind paths/identities, retention, capacity, original signing
   digest, immutable source, rollback owner and maintenance/recovery deadlines.
   Offline comparison and real session/browser/capacity acceptance remain distinct.

Use the attached actual observations and tested Forge comparison/checker as inputs.
The helper checker checks shape only; independently verify all source and receipt
bytes and keep their lineage. Preserve sealed Request029–032 and every attempt.
All21 production evidence slots stay null; production compiler/controller remain
disabled. Return final source archive/hash/commit (no push), complete mapping ledger
with no unexplained placeholders, exact VM108 parity and changed-byte tests, plus
one coherent bounded production proposal and explicit remaining acceptance gates.
No request to Patrick for production authority until that concrete packet exists.
