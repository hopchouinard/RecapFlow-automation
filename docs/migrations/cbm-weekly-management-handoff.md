# CBM-WEEKLY-20260911-015 — approved weekly operations

Patrick approved the operational transition: US$5/week on the dedicated production
OpenRouter key, automatically renewed service credentials rather than permanent
tokens, and resolving the stopped runner. His required interaction is one form
submission with meeting metadata/Fathom ID and Zoom chat (or uploaded transcript).
There must be no subsequent routine approval/command to acquire, generate all
Markdown, index into LanceDB and obtain paired recovery acceptance. Publication,
historical replacement, retirement and broader migration gates remain unchanged.
No real recording/model exercise is requested by this maintenance change.

## 1. Reconcile the latched runner before resuming

Forge observed attention_required on September11. attention.json only says
`automatic_execution_requires_review`; its timestamp is overwritten on every tick
and does not establish onset. No automation execution.log currently exists.
An explicit existing scan('next') in the installed effective-s01 helpers returned
completed=[],next=null,attention=false at inspection. Visible job remains the old
September8 run. OpenRouter usage remains0.392107166,remaining1.607892834 under$2.
Do not infer root cause or clear attention from that one scan alone. scan('next')
can fence expired stages; do not describe it as universally read-only.

Inspect private cron/boot/Docker/DB/network/management evidence, active containers,
all automatic-policy jobs/stages/attempts, started journals, checkpoint/queue and
boot identity. Preserve original marker and evidence. Repair the demonstrated
fault; validate active pinned scanner and host under real cron environment. Only
reconcile the latch under ordered runner/manual/submission locks after excluding
uncertain side effects or pending recovery. Preserve any uncertain job state;
never blindly retry a provider call or old outbox event. Observe at least3 healthy
scheduled ticks after controlled resume. Record root cause if evidence supports
one; otherwise explicitly label unreproduced transient/unknown and preserve the
limitation. Add safe durable diagnostic visibility if needed, not raw secrets.

## 2. Coordinated US$5 weekly provider policy

Verified immutable TWO-FILE overlay on VM109:
`/srv/community-brain/workspaces/cbm-weekly-20260911/`.
packet-manifest.json matches companion cbm-weekly-manifest.json. It contains only
bounded_worker.py and indexing_budget.py from the automatic helpers. Merge these
into a new effective automatic packet retaining every existing effective-s01
management fix and source pin; never replace the full directory with this overlay.
Keep original packets. Update launcher/renderer/runtime/recovery inventory pins
and preserve private before/after controls with verified off-host copies.

While runner is held and no worker is active, update the EXISTING dedicated
production OpenRouter key via management authority: limit=5,limit_reset=weekly,
include_byok_in_limit=true,not a management key. Do not top up account credits or
change account auto-recharge, other keys, provider models, or client scopes.
Verify actual GET /api/v1/key metadata with the scoped key. No paid request needed.
The new guards require finite numeric remaining in(0,5], exact$5 weekly policy,
BYOK accounting and a non-management key. Per-job request ceilings, durable
journals, single-writer locks and outcome-unknown stops remain. Weekly allowance
reset must never reset a stage attempt or restart a failed job automatically.

50 targeted tests passed (weekly provider guard acceptance/rejection and rollover,
per-job ceiling surviving reset, host checkpoint/attention/orphan gates and
existing indexing journal behavior). No live model call made. Original manual
rehearsal packets retain their$2 lifetime policy and must not be repurposed.

## 3. Automatic service-token renewal

Use existing Infisical authority and Mac maintenance scheduler. Retain separate
scopes/audiences and seven-day lifetimes; renew at least48h before expiry. Include
OpenWebUI retrieval, Mac collector, manual operator and read/metrics probes.
Inventory actual active consumers before rotation; do not infer from old receipts.
AuthentiK human login/session/OIDC policy remains unchanged. Fathom/OpenRouter
keys are not these September17 service tokens. Do not remove expiration.

Implement idempotent persisted renewal state: provision new scoped token in
Infisical, render API acceptance with short old/new overlap, deliver new token to
its exact consumers, verify authenticated access and cross-scope denial, then
remove old acceptance and verify old token rejection. Do not revoke the old token
on partial consumer failure or uncertain effects. Keep secrets out of handoff,
logs and repository; use existing secure channels and runtime permissions.

Update current API renderer and recovery controls to use fresh authority and
current Signal frontend/backend pins during recreation. Preserve CA trust after
OpenWebUI recreation. Serialize rotation with existing quiet/management locks;
no duplicate scheduler. Provide alerts for renewal failures and expiry approaching
without successful consumer acceptance. Exercise clock-driven renewal scheduling,
crash/partial delivery recovery, overlap and revocation with fixtures; verify one
controlled live renewal of active consumers without jobs/model calls. Record
actual resulting expiries and next renewal due. Do not claim future scheduling
verified merely from a script's presence.

## Completion

Return safe hashed receipts for runner reconciliation/limitations, actual budget,
renewal tests and live consumer checks, observed ticks, current pins and verified
private before/after recovery controls. Preserve database/corpus/archive/model-call
counts except explicit credential/operational metadata; no new jobs. Confirm
one-click form automatic policy stays enabled,87 meetings/481 files and1901 FTS
rows (compare actual before/after if user submitted a new meeting meanwhile).
Next fresh real meeting remains the first production end-to-end automatic job
acceptance; do not fabricate it. If blocked, state exactly what is missing.
No further permission from Forge/Patrick is pending for this approved scope.
