# Request016 — close weekly-operation findings

Patrick authorized weekly operation readiness, fixes and token renewal in request015.
Forge verified/acknowledged015; do not replay rotation, budget mutation or activation.
This follow-up resolves two findings explicitly reported in its completion receipt.

1. Diagnose and repair the failing Mac legacy-intake-ownership hook. Establish the
actual runtime failure from private evidence and validate the corrected check in
the scheduled environment. Preserve Zoom Folder Actions disabled, old n8n stopped,
legacy retrieval paused and terminal ownership guards. Do not remove/bypass the
check, reactivate legacy writers, retire services or extend rollback deadlines.
Return exact nonsecret cause, fix/test evidence and a successful scheduled cycle.
If resolution needs a broader change, report the concrete blocker before doing it.

2. Correct automatic indexing journal metadata. Companion indexing_budget.py is
an immutable one-file overlay: compared with015 it ONLY changes the new journal
header from lifetime_limit_usd:2 to limit_usd:5,limit_reset:weekly. Hash it against
request manifest. The US$5 weekly request guard is unchanged.51 targeted tests
pass, including new header verification. Keep existing journals untouched.

Merge into a new effective weekly packet preserving ALL current management fixes,
including automatic_host.py first-failure preservation. Use ordered quiet/scheduler
locks, update launcher/runtime/renderer/recovery pins together, and preserve private
before/after controls off-host. Current Signal frontend and fresh Infisical tokens
must remain authoritative; no rollback to old tokens or earlier effective packet.
Check no eligible jobs before changing runner; do not accidentally trigger paid work.

Verify unchanged DB/managed files/provider usage, automatic capability, idle healthy
runner, live retrieval/probes,51-test Forge source evidence plus appropriate management
tests and scheduled maintenance cycle. No model requests or synthetic jobs needed.
Do not rotate tokens unnecessarily; next actual renewal remains scheduler-owned.
Return hashed safe receipts with findings, limitations, checks and effective pins.
Publication, replacement, retirement and new migration phases remain gated.
