# Request031: capture controller host integration, development only

Request ID: CBM-CAPTURE-CONTROLLER-20260921-031. Target: home.servers.
Deadline:2026-09-21T20:00:00Z. Request030 was consumed and acknowledged.

Patrick instructed proceeding with the next task. Existing Mac administration,
read-only production inspection, synthetic VM108 administration and shared relay
are authorized. This request does not authorize protected capture/transfer, copying
production data/signing material into dev, production stop/restart/renewal, installation,
cutover, processing resume, VM resize or paid provider work. VM101 remains recovery-only.

Forge implemented/tested the attached operation and component-receipt core. Import
its exact source alongside unchanged Request030 libraries; keep lineage explicit.
Do not represent the local signal timer as a remote maintenance guarantee.

Implement and validate the remaining Mac/SSH orchestration on fresh synthetic VM108
state: persistent owner and operation IDs, specification/source bindings, existing
mutex/holds, per-phase absolute deadlines, detached target readback, loss of SSH and
loss of DB fence, no blind replay, exact incumbent stop/restart and restoration on
bounded maintenance expiry. A killed local caller must not strand synthetic serving
indefinitely or allow a competing operation. Preserve all failed/uncertain attempts.
Test actual process/connection interruption, not mocked success flags. Existing
Request029/030 fixtures remain untouched. No old validation_host checkpoint mutation.

Implement a component-based validator for independently restored database evidence:
pin dump and before/after observations, capture ID and source manifests, independent
server identities, canonical schema/table/sequence/relationship observations and
file references. Reject missing/extra/mismatched evidence, stale or mixed attempts,
unknown acceptance flags and development receipts offered as production proof.
Integrate it into a preparation-only candidate contract while leaving both existing
production compiler and controller refusals in place and all21 production slots
unfilled. The Forge receipt checker verifies files only; it is not DB acceptance.

Return exact source archive/manifest/commit (no push), VM108 source parity, positive
and negative execution receipts, retained paths and dispositions, and an updated
concrete operator proposal. Identify any incomplete controller guarantee explicitly.
Refresh read-only deadlines/identity metadata if needed; never rotate to make a
proposal pass. Production phase authorization remains a later user decision after
this tested implementation is concrete and reviewable. No private content in relay.
