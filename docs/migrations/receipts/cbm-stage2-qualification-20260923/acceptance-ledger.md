# Request034 stage 2 acceptance ledger

Request: `CBM-STAGE2-QUALIFICATION-20260923-034`. Scope: isolated VM108 and development authority only. Source baseline: Request033 `41eef6ed3bfcd53d4954803581dcae084a19320f` and Forge source manifest `0eec32da7770ff652a2193f87b97c858b334673eacf45820098cfc0697c980c1`. This is a qualification ledger, not production approval. No VM109 install, production renewal, protected transfer, VM101 restart, paid provider call, or VM resize occurred.

## Proven in the isolated dev scope

- Actual Infisical project `homelab`, path `/development/community-brain-dev/request034`: five generated identities, read/save/read equality, unchanged parent development material, no production key read/change. A fresh Step CA NATS leaf was issued and its key pair/chain/SAN verified; dev NATS accepted it. The development-only leaf digest is `46b0b43fdff9d0d9d3cda4213b6820aca6dc3ddb8a393b9144cb339b53ff2090` and expires 2026-09-24T22:50:50Z.
- Pinned API image `sha256:6e7f43ebd7970f89ae9f1afe5d4d77b89448e188e4a580ff9e38bac923d5bc5b` and WebUI image `sha256:08046b9748558bc2747dd20c9c77fc0e6b05216b8ad33513e7a9152cea15b87b` served a synthetic corpus. The actual WebUI filter retrieved a source. Mac development collector configuration, Kuma and Prometheus consumers were independently probed. An intentionally lost renewal delivery acknowledgment resumed the same Infisical generation; old and new identities overlapped for consumer readback, and old credentials were revoked only after acceptance. Current dev expiry is 2026-09-30T22:57:49Z. An isolated exact-image API returned 200 for current, 401 for expired, 401 for wrong and 403 for cross-scope credentials.
- A synthetic job completed actual processing and indexing; duplicate execution was refused. The initial paired independent PostgreSQL and durable-state restore matched its source observations and verified 72 state files. A fresh post-browser pair then matched canonicalized database/state observations and verified 114 files, including 35 LanceDB vector files by exact hashes (vector manifest `54f0d860f5db50aba74b971306e4e7ed12a45999c946014077336e522403c391`); FTS showed 17 indexed and zero unindexed rows. Its recovery manifest is `3148078f797e5513edf00aafc6fdbce279e4709b5bf90abf843bc574ab2652e0`. The restored exact-image WebUI retained the synthetic chat and attachment, accepted the original synthetic signing-key session with HTTP 200 and rejected that same token under an isolated wrong key with HTTP 401. Signup was disabled in both restored copies. VM108 browser acceptance covered rendered login, synthetic chat and upload retention after reload and WebUI recreation, source display, and a rendered sign-in-only page after persisted signup disable; signup POST returned 403. See `BROWSER-OBSERVATION.md`.
- The declared 4-GiB VM108 concurrent profile used two API clients, one WebUI action/retrieval client, an actual synthetic worker, and a separate PostgreSQL/state restore. Across 75 seconds it completed 677 request pairs with zero failures, p95 516.4 ms, max 767.3 ms, minimum available memory 1,768,036 KiB (44.2% of baseline), peak observed container CPU 52.32%, and zero kernel OOM kills. All four workload types overlapped for 4.8 seconds. This satisfies that bounded profile, not a longer continuous four-way restore or real browser chat load. No resize evidence was needed for this profile.
- The final immutable VM108 `packet-v12` has manifest SHA-256 `7c5cfba2187e72ea40a6955592278539f7982376abbe647819e466354b3b3825` and archive SHA-256 `f28a4b5d2ffce18d10c56c60ec26fcad528f1d3f7a0a66f1182480a44d9b5a22`. All 16 changed-source checks passed on VM108. A final same-packet readback verified all 20 pinned API image files, no old overlays, and the 17/0 FTS state. The admission review core requires an externally pinned plan digest, fresh authority, exact VM109/mount/incumbent/hold/lock bindings, and exactly 21 production evidence names, while refusing any enable switch. It is a refusal gate, not a production controller.

## Failed or interrupted attempts retained

- A manual submission under the real quiet lease was correctly refused; a fresh, separately named attempt after disposable boot reconciliation passed. The original intent remains.
- The first isolated negative-auth API attempt failed its own readiness lookup; a new attempt with corrected address handling passed the four HTTP outcomes. The earlier marker remains.
- The first signup-off verification timed out while WebUI cold-started. Later independent readback passed, with no restart or OOM kill.
- The first post-browser restore used the already occupied `integration_restore` test database name and stopped before applying that restore. Its captured files and marker remain; the fresh `integration_restore_postbrowser_v2` attempt passed separately.
- The exact WebUI image's built-in health command initially reported `unhealthy` with a `jq` error during its long cold start, even after `/health` and rendered browser behavior returned HTTP 200. It later became `healthy` without a health override. The initial timeout remains recorded rather than treated as an immediate pass.

## Unavailable or unqualified

- A bounded production-capable controller, installer, independent VM109 finalizer/rollback, and VM108 fault acceptance for restart/kill, SSH loss, failed finalizer, competing writers, expiry and uncertain readback were **not completed**. `production_admission.py` is only a fail-closed review core. The production execution path is **not reviewable for execution**. Request033 production compiler/controller refusals and all 21 null production evidence slots remain unchanged.
- The five production service identities expired 2026-09-22T20:53:07Z. The last read-only Mac status shows renewal failed with `AssertionError` in the existing renewal policy. Request034 did not renew them or extend authority. VM109 still has the original healthy API and Alloy containers on the expected state-mount UUID.
- Dev receipts do not prove protected production database/corpus/WebUI restoration, real sessions, production CA/consumer credentials, off-host recovery, persistent production installation or boot finalization. Real paid-provider/spend-budget acceptance and Mac desktop collector intake/upload were not exercised; the provider served deterministic synthetic responses. VM101 remained recovery-only.

The original, partial and replacement development attempts are retained in the private VM108/Mac workspaces. After acceptance, all seven Request034 development containers were stopped with volumes retained; the four pre-existing VM108 development containers kept their IDs and remained running. Production promotion, preservation, serving cutover and processing resume each require their own reviewed and separately authorized phase after the missing controller and production receipts are resolved.
