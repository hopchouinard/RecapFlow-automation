# Request032 verification receipts

Development work completed at commit `dd001c2dcc9338f64e1af0c18fbb1694b183025f`. No push or parent gitlink update.
All 49 final source files match VM108 exactly. Manifest SHA-256:
`48675b902dceb1d8ba4fb43e4a362dca0ee68177801431d25ba874ce0ebf01b7`.

- Actual scheduler, manual and capture entry points share the existing Mac mutex and durable journal. Mac/SSH death, competing processes, cross-kind blocking, legacy conflicts, authoritative recovery and refusal to replay passed on final source.
- A real failed finalizer kept admission closed. Ordered boot/startup/probe units refused the dependent effect until recovery succeeded. This tests the startup path on the existing kernel boot, not an actual VM reboot.
- Independent PostgreSQL18.6 capture/restore passed with live DML, DDL and sequence clients. 46 distinct evidence negatives include 15 actual reverted catalog mutations. An unmanaged administrative backend also caused safe capture refusal.
- 22 readback/authentication negatives, including stale/foreign/tampered evidence and an unknown SSH host key, were rejected; 3 actual changed/extra/bytecode source subprocesses refused admission.
- Mac fixture 50, Mac admission 9, VM108 recovery 49, VM108 admission 9, existing activation 19 and parent inventory 293 tests passed. The five Mac metadata fixtures are explicitly adapted; actual native metadata rejection remains tested.
- All 83 pre-existing dev containers and two production containers retain baseline identity, configuration and runtime state. Production holds and retained bytecode match. Installed Mac scheduler, mutex and legacy pending state remain unchanged. VM101 was untouched.
- Both existing production refusals remain intact; all 21 production slots remain null.

All 22 operation attempts, 32 prepared specs, 33 fixtures and six source packets are retained. Ten prepared specs never dispatched; they remain expired under the retired root. All 22 shared journal intents are resolved with retained proofs. Only 35 recorded Request032 containers were stopped; none or their data were removed. Original legacy pending is explicitly pinned as retired synthetic state, not silently cleared or migrated.

Historical preparation `manual1` failed its HTTP health check before specification/admission or serving stop, and is retained. Final verification driver mistakes (missing template argument and an incorrect spec path) failed before evidence validation and were corrected; the successful database receipt identifies the actual final source. The initial activation-suite path was corrected to the isolated worktree. The cleanup runner also encountered dependent transient units already unloaded by systemd; a follow-up verified inactive/absent units and preserved all data. These runner issues are not counted as product successes.

Production execution is not ready. Remaining gaps are production helper mappings, catalog coverage, persistent boot/service bindings and protected private restore/acceptance contracts. See OPERATOR-PROPOSAL.md. Recovery depends on available control storage, Docker and PostgreSQL; unconditional availability is not claimed.

Read-only expiry metadata at 2026-09-21T18:16:45Z still showed 2026-09-22T20:53:07Z. No production renewal, rotation, installation, maintenance, capture, transfer, cutover, resume, resize or provider spending occurred. No production private content or signing material is included.

Machine receipts: verification-results.json, vm108-audit.json, preservation.json, teardown.json, final interruption/startup/database/readback/source checks and artifact-manifest.json. Exact source: shared-admission-source.tar.gz, source-manifest.json, LINEAGE.json and source-commit.patch.
