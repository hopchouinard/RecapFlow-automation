# Next acceptance: a selected meeting through live indexing

Dark mode and the real September8 recap previews/downloads are accepted.
Manual writer/intake ownership, paired restore and monitoring have passed.
The remaining CBM-08 execution gap is a real eligible meeting indexed into the
production corpus and retrieved through the accepted user path. Disposable
synthetic indexing passed; it does not establish live indexing acceptance.

## User clarification and revised sequencing

Patrick clarified that every existing meeting is already ingested; there is no
unindexed historical meeting to select. The live corpus was freshly rechecked:
87 sessions,1901 rows, all13 corpus files match the installed checkpoint exactly.
Do not request an arbitrary new meeting or reingest a date merely to complete a
test. The live new-meeting indexing observation waits for a genuinely new meeting;
it is not a prerequisite for preparing other independently approved migration work.
Existing session replacement remains separately gated. The procedure below is
retained for the next naturally occurring selected meeting, not a current blocker
requiring Patrick to find missing data.

## Required selection (when a new meeting exists)

Patrick must select a meeting not already in the corpus, providing its Fathom
link/ID, date/timezone and matching Zoom chat file or relative folder path.
Reuse already verified metadata only when the selected identity matches exactly.
Verify that its local date has no canonical session or conflicting reservation
before fetching content or spending model allowance. Existing September8 remains
protected; choosing a replacement requires its own concrete replacement plan
and explicit approval, not a bypass of the collision guard.

## Execution once selected

1. Bind one reviewed meeting identity, metadata lookup window and selected chat
   hash. Use the restricted manual collector and acquisition-only Fathom delivery.
   Adapt the fixture-specific submission/acquisition guard to that identity and
   verify it with fake providers before deployment; never reuse September8's
   fixed client or broaden the metadata lookup to unrelated recordings.
2. Verify current provider allowance, scope, corpus/checkpoint and no competing
   worker. Last reconciled remainder was US$1.607892834; recheck rather than
   assuming it is current. Retain the US$2 lifetime cap with no top-up/reset.
   Use the existing20 processing/32 indexing request ceilings and stop on unknown
   outcomes or insufficient allowance. Do not start model work before selection.
3. Submit only the selected job and run explicit acquisition and processing
   selections. Verify durable receipts and each authenticated artifact hash.
   Review the artifacts-to-index transition and date reservation before invoking
   the selected indexing stage; keep the three unrelated pending events inert.
4. Verify actual indexed row outcomes, full FTS coverage, original-session
   invariance, retrieval sources/context and settled provider accounting. Preserve
   partial/unknown evidence without blind retries. Never silently replace a date.
5. Request management's POST-MANUAL-JOB.md paired DB/files/config/corpus/runtime
   checkpoint and restore verification through the existing relay. Ask Patrick
   to verify a relevant question through his real Open WebUI session.

No new recording has been selected and no additional call or indexing event was
executed by preparing this document. Publication and recurring polling remain
disabled. Two real weekly cycles and the first scheduled evening backup execution
still require observation; do not claim stabilization or retire legacy services.
CBM-09 remains a separate phase and CBM-10/Q-01 through Q-06 remains the last
whole-migration step. The next user input is meeting selection, not routine
permission to repeat completed checks.
