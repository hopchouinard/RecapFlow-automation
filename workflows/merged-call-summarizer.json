{
  "id": "5",
  "name": "Merged Call Summarizer",
  "nodes": [
    {
      "id": "e1000000-0000-4000-8000-000000000001",
      "name": "Manual Trigger",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        0,
        300
      ],
      "parameters": {}
    },
    {
      "id": "e1000000-0000-4000-8000-000000000002",
      "name": "Code: Validate and Check Partner",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        250,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst watchDir = '/home/node/watch';\n\n// Find all date prefixes that have both a chat and transcript file\nconst files = fs.readdirSync(watchDir);\nconst dateMap = {};\nfor (const f of files) {\n  const m = f.match(/^(\\d{4}-\\d{2}-\\d{2})/);\n  if (!m) continue;\n  const date = m[1];\n  if (!dateMap[date]) dateMap[date] = {};\n  if (f.includes('-zoom-chat') && f.endsWith('.txt')) dateMap[date].chat = f;\n  if (f.includes('-transcript') && f.endsWith('.txt')) dateMap[date].transcript = f;\n}\n\n// Pick the most recent date that has both files\nconst complete = Object.entries(dateMap)\n  .filter(([, v]) => v.chat && v.transcript)\n  .sort(([a], [b]) => b.localeCompare(a));\n\nif (complete.length === 0) {\n  throw new Error('No complete file pairs found in watch directory (need both YYYY-MM-DD-zoom-chat.txt and YYYY-MM-DD-transcript.txt)');\n}\n\nconst [datePrefix, { chat, transcript }] = complete[0];\nconst chatFile = `${watchDir}/${chat}`;\nconst transcriptFile = `${watchDir}/${transcript}`;\n\nconst chatText = fs.readFileSync(chatFile, 'utf8');\nconst transcriptText = fs.readFileSync(transcriptFile, 'utf8');\n\nreturn {\n  json: {\n    datePrefix,\n    chatText,\n    transcriptText,\n    chatFile,\n    transcriptFile\n  }\n};"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000003",
      "name": "Code: Merge Content",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        500,
        300
      ],
      "parameters": {
        "jsCode": "const mergedText = `# MEETING TRANSCRIPT\\n\\n${$json.transcriptText}\\n\\n# ZOOM CHAT LOG\\n\\n${$json.chatText}`;\n\nreturn {\n  json: {\n    ...$json,\n    mergedText\n  }\n};"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000004",
      "name": "Code: Create Output Folder",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        750,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst datePrefix = $json.datePrefix;\nconst outputDir = `/home/node/output/${datePrefix}`;\nif (!fs.existsSync(outputDir)) {\n  fs.mkdirSync(outputDir, { recursive: true });\n}\nreturn { json: { ...$json, outputDir } };"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000005",
      "name": "Code: Save transcript.txt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1000,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $json.outputDir;\nfs.writeFileSync(`${outputDir}/transcript.txt`, $json.transcriptText, 'utf8');\nreturn { json: { ...$json } };"
      }
    },
    {
      "id": "ef000000-0000-4000-8000-0000000000f1",
      "name": "Code: Split Signal",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1250,
        300
      ],
      "parameters": {
        "jsCode": "const cfg = $('Code: Pipeline Config').first().json.steps.signalMap;\nconst src = $('Code: Validate and Check Partner').first().json.transcriptText;\nconst halving = $input.first().json.halving || 0;\nconst target = Math.floor(cfg.chunkTargetTokens / Math.pow(2, halving));\n\nconst estimateTokens = (s) => Math.ceil(s.length / 3.6);\n\nfunction splitTranscriptByLines(text, targetTokens) {\n  const lines = text.split('\\n');\n  const chunks = [];\n  let buf = [], bufTokens = 0;\n  for (const line of lines) {\n    const t = estimateTokens(line) + 1;\n    if (bufTokens + t > targetTokens && buf.length > 0) {\n      chunks.push(buf.join('\\n'));\n      buf = []; bufTokens = 0;\n    }\n    buf.push(line); bufTokens += t;\n  }\n  if (buf.length && buf.join('\\n').trim()) chunks.push(buf.join('\\n'));\n  return chunks;\n}\n\nconst SYSTEM = `You are analysing ONE PART of a longer coaching-call transcript. Extract the signal from this part only — the information that would help a future reader understand what was discussed, who said what, and what artefacts or resources were referenced.\n\nEmit a markdown document using H2 headings drawn from exactly these six lowercase slugs, in this order: general, insights, qa, tools, links, decisions. Omit a section entirely if this part contains nothing for it. Do not invent new sections.\n\n## general\nA short narrative of what this part covered. Factual, not promotional.\n\n## insights\nBullet points of the most valuable takeaways surfaced in this part. Each bullet self-contained. Attribute the speaker when it matters.\n\n## qa\nPairs of questions and answers, formatted:\n**Q (Questioner):** ...\n**A (Answerer):** ...\nInclude only exchanges where both sides had real content.\n\n## tools\nBullet list of tools, products, frameworks, platforms or services mentioned. One line each, with a 5–15 word note on context.\n\n## links\nBullet list of URLs, repositories, documents or resources shared. One line each with a brief descriptor.\n\n## decisions\nBullet list of commitments, action items or decisions. Phrase each as actor + action.\n\nDo not invent content not present in this part. Results from all parts are merged downstream.`;\n\nreturn splitTranscriptByLines(src, target).map((chunk, i) => ({\n  json: {\n    stepName: 'signal.map',\n    model: cfg.model,\n    maxTokens: cfg.maxTokens,\n    reasoningEffort: cfg.reasoningEffort,\n    expect: 'signal.map',\n    chunkIndex: i,\n    system: SYSTEM,\n    user: chunk,\n  },\n}));"
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "openrouterCall",
          "mode": "list",
          "cachedResultName": "OpenRouter Call"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "ef000000-0000-4000-8000-0000000000f2",
      "name": "Execute: Signal Map via OpenRouter",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        1400,
        300
      ]
    },
    {
      "id": "ef000000-0000-4000-8000-0000000000f3",
      "name": "Code: Build Signal Reduce",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1550,
        300
      ],
      "parameters": {
        "jsCode": "const cfg = $('Code: Pipeline Config').first().json.steps.signalReduce;\nconst items = $input.all().map((i) => i.json);\nconst failed = items.filter((i) => !i.ok);\nif (failed.length) {\n  const f = failed[0];\n  throw new Error(`Signal map failed on chunk ${f.chunkIndex} after ${f.attempts} attempts (failureKind=${f.failureKind}). No artifact written.`);\n}\n\nconst chatText = $('Code: Validate and Check Partner').first().json.chatText || '';\nconst ordered = items.slice().sort((a, b) => a.chunkIndex - b.chunkIndex);\nconst parts = ordered.map((i, n) => `### PART ${n + 1}\\n\\n${i.text}`).join('\\n\\n');\n\nconst SYSTEM = `You are merging per-part signal extractions from a single coaching call into one consolidated document, and folding in the call's chat log.\n\nEmit a markdown document with exactly six H2 sections, using these literal lowercase slugs in this order:\n\n## general\n## insights\n## qa\n## tools\n## links\n## decisions\n\nRules:\n- general: write a 2–4 paragraph narrative covering the WHOLE call, synthesized across all parts — not a concatenation of the per-part summaries.\n- insights, qa, decisions: merge across parts, preserving chronological order and dropping duplicates.\n- tools and links: the chat log is the primary source for these. Merge chat-log entries with those found in the parts, and DEDUPE aggressively — the same URL or tool must appear once.\n- Omit a section entirely if there is genuinely no content for it.\n- Do not invent content that is not present in the parts or the chat log.\n\nSIZE BUDGET: the complete document must not exceed approximately ${cfg.budgetTokens} tokens (roughly ${cfg.budgetTokens * 4} characters). If the material exceeds that, cut the weakest insights and least-useful Q&A first. Never cut links or tools to fit.\n\nCRITICAL: output the sections in EXACTLY the order listed above. Do not re-order, merge or rename them.`;\n\nreturn [{\n  json: {\n    stepName: 'signal.reduce',\n    model: cfg.model,\n    maxTokens: cfg.maxTokens,\n    reasoningEffort: cfg.reasoningEffort,\n    expect: 'signal.reduce',\n    chunkIndex: 0,\n    system: SYSTEM,\n    user: `# PER-PART EXTRACTIONS\\n\\n${parts}\\n\\n# ZOOM CHAT LOG\\n\\n${chatText}`,\n  },\n}];"
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "openrouterCall",
          "mode": "list",
          "cachedResultName": "OpenRouter Call"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "ef000000-0000-4000-8000-0000000000f4",
      "name": "Execute: Signal Reduce via OpenRouter",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        1700,
        300
      ]
    },
    {
      "id": "ef000000-0000-4000-8000-0000000000f5",
      "name": "Code: Aggregate Signal",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1850,
        300
      ],
      "parameters": {
        "jsCode": "const CANON = ['general', 'insights', 'qa', 'tools', 'links', 'decisions'];\nconst items = $input.all().map((i) => i.json);\nconst failed = items.filter((i) => !i.ok);\nif (failed.length) {\n  const f = failed[0];\n  throw new Error(`Signal reduce failed after ${f.attempts} attempts (failureKind=${f.failureKind}). No artifact written.`);\n}\n\n// Normalize heading level to H2 (spec 4.6: the model emits H1 on roughly half of runs).\nlet md = items[0].text.replace(/^#{1,3}[ \\t]+([a-z]+)[ \\t]*$/gm, (m, slug) =>\n  CANON.includes(slug) ? `## ${slug}` : m);\n\nconst found = [];\nconst re = /^##[ \\t]+([a-z]+)[ \\t]*$/gm;\nlet m;\nwhile ((m = re.exec(md)) !== null) if (CANON.includes(m[1])) found.push(m[1]);\nconst missing = CANON.filter((s) => !found.includes(s));\nif (missing.length) {\n  throw new Error(`extracted-signal is missing required section(s): ${missing.join(', ')}. No artifact written.`);\n}\n\nreturn [{ json: { signalText: md, totalCost: items.reduce((s, i) => s + ((i.usage && i.usage.cost) || 0), 0) } }];"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000008",
      "name": "Code: Save extracted-signal.md",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1500,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $('Code: Create Output Folder').item.json.outputDir;\nconst signalText = $json.signalText;\nfs.writeFileSync(`${outputDir}/extracted-signal.md`, signalText, 'utf8');\nreturn { json: { signalText, outputDir, datePrefix: $('Code: Create Output Folder').item.json.datePrefix } };"
      }
    },
    {
      "id": "ef000000-0000-4000-8000-0000000000f6",
      "name": "Code: Split Post Sections",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1750,
        300
      ],
      "parameters": {
        "jsCode": "const cfg = $('Code: Pipeline Config').first().json.steps.postSection;\nconst md = $input.first().json.signalText;\n\nconst CANON = ['general', 'insights', 'qa', 'tools', 'links', 'decisions'];\nfunction splitSignalIntoSections(src) {\n  const out = {};\n  const re = /^#{1,3}[ \\t]+([a-z]+)[ \\t]*$/gm;\n  let m; const marks = [];\n  while ((m = re.exec(src)) !== null) marks.push({ slug: m[1], start: m.index, bodyStart: re.lastIndex });\n  for (let i = 0; i < marks.length; i++) {\n    const end = i + 1 < marks.length ? marks[i + 1].start : src.length;\n    const body = src.slice(marks[i].bodyStart, end).trim();\n    if (CANON.includes(marks[i].slug) && body) out[marks[i].slug] = body;\n  }\n  return out;\n}\n\nconst BRIEF = {\n  general:   'Write ONE short paragraph summarizing the overall value and themes of the call.',\n  insights:  'Summarize the strongest practical or strategic takeaways as short lines, one per takeaway.',\n  qa:        'Present the most useful question-and-answer pairs clearly and concisely, one exchange per block.',\n  tools:     'List the important tools, frameworks and concepts with a short explanation each, one per line.',\n  links:     'Present the most useful links and resources in a clean, easy-to-scan way. Write URLs as plain text.',\n  decisions: 'List the open questions, follow-ups and commitments worth revisiting, one per line.',\n};\n\nconst sections = splitSignalIntoSections(md);\nreturn CANON.filter((s) => sections[s]).map((slug, i) => ({\n  json: {\n    stepName: `post.section.${slug}`,\n    section: slug,\n    model: cfg.model,\n    maxTokens: cfg.maxTokens,\n    reasoningEffort: cfg.reasoningEffort,\n    expect: 'post.section',\n    chunkIndex: i,\n    system: `You are writing ONE section of a polished community post about a weekly AI community call, for members who may not have attended live.\n\n${BRIEF[slug]}\n\nIMPORTANT: This post is published on Skool, which does NOT render markdown. Output PLAIN TEXT only. Do NOT use #, **bold**, - bullets, or [links](url). Use line breaks and spacing for readability.\n\nOutput ONLY this section's body text. Do NOT write a heading — the heading is added automatically. Do not invent information not present in the input. Keep it concise, skimmable and ready to copy-paste.`,\n    user: sections[slug],\n  },\n}));"
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "openrouterCall",
          "mode": "list",
          "cachedResultName": "OpenRouter Call"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "ef000000-0000-4000-8000-0000000000f7",
      "name": "Execute: Post Sections via OpenRouter",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        1875,
        300
      ]
    },
    {
      "id": "ef000000-0000-4000-8000-0000000000f8",
      "name": "Code: Assemble Post",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2000,
        300
      ],
      "parameters": {
        "jsCode": "const HEADERS = {\n  general:   '📝 SUMMARY',\n  insights:  '💡 KEY INSIGHTS',\n  qa:        '❓ KEY Q&A',\n  tools:     '🛠️ TOOLS AND CONCEPTS MENTIONED',\n  links:     '📎 SHARED RESOURCES',\n  decisions: '🔄 FOLLOW-UPS WORTH EXPLORING',\n};\nconst ORDER = ['general', 'insights', 'qa', 'tools', 'links', 'decisions'];\n\nconst items = $input.all().map((i) => i.json);\nconst failed = items.filter((i) => !i.ok);\nif (failed.length) {\n  const f = failed[0];\n  throw new Error(`Community post section '${f.section}' failed after ${f.attempts} attempts (failureKind=${f.failureKind}). No artifact written.`);\n}\n\nconst bySection = new Map(items.map((i) => [i.section, i.text.trim()]));\nconst post = ORDER\n  .filter((s) => bySection.has(s) && bySection.get(s))\n  .map((s) => `${HEADERS[s]}\\n\\n${bySection.get(s)}`)\n  .join('\\n\\n\\n');\n\nreturn [{\n  json: {\n    communityPostText: post,\n    totalCost: items.reduce((sum, i) => sum + ((i.usage && i.usage.cost) || 0), 0),\n  },\n}];"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000011",
      "name": "Code: Save community-post.md",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2000,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $('Code: Create Output Folder').item.json.outputDir;\nconst communityPostText = $json.text;\nfs.writeFileSync(`${outputDir}/community-post.md`, communityPostText, 'utf8');\nreturn { json: { communityPostText, outputDir, datePrefix: $('Code: Create Output Folder').item.json.datePrefix } };"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000012",
      "name": "Code: Build Compress Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2250,
        300
      ],
      "parameters": {
        "jsCode": "const cfg = $('Code: Pipeline Config').first().json.steps.compress;\nconst communityPostText = $input.first().json.communityPostText;\n\nconst SYSTEM = `Condense the community post below by about 30 to 40 percent while preserving all high-value information.\n\nIMPORTANT: This post will be published on Skool, which does NOT render markdown. Output PLAIN TEXT only. Do NOT use markdown syntax like # headers, ** bold **, - bullet lists, or [links](url). Keep the same emoji-based section headers and plain text formatting as the input.\n\nCRITICAL: You MUST preserve the section order EXACTLY as it appears in the input — do not rearrange sections:\n1. 📝 SUMMARY\n2. 💡 KEY INSIGHTS\n3. ❓ KEY Q&A\n4. 🛠️ TOOLS AND CONCEPTS MENTIONED\n5. 📎 SHARED RESOURCES\n6. 🔄 FOLLOW-UPS WORTH EXPLORING\n\nGoals:\n- make it easier to skim\n- remove repetition\n- keep links, useful Q&A, and strongest insights\n- preserve a professional, community-friendly tone\n- maintain plain text formatting with emoji section headers\n- preserve the section order listed above\n\nDo not remove important resources or key takeaways.\n\nHere is the draft post:`;\n\nreturn [{\n  json: {\n    stepName: 'compress',\n    model: cfg.model,\n    maxTokens: cfg.maxTokens,\n    reasoningEffort: cfg.reasoningEffort,\n    expect: 'none',\n    chunkIndex: 0,\n    system: SYSTEM,\n    user: communityPostText,\n  },\n}];"
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "openrouterCall",
          "mode": "list",
          "cachedResultName": "OpenRouter Call"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "e1000000-0000-4000-8000-000000000013",
      "name": "Execute: Compress via OpenRouter",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        2375,
        300
      ]
    },
    {
      "id": "ee000000-0000-4000-8000-0000000000e5",
      "name": "Code: Unwrap Compress",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2450,
        300
      ],
      "parameters": {
        "jsCode": "const items = $input.all().map((i) => i.json);\nconst failed = items.filter((i) => !i.ok);\nif (failed.length) {\n  const f = failed[0];\n  throw new Error(`Step ${f.stepName} failed after ${f.attempts} attempts (failureKind=${f.failureKind}). No artifact written.`);\n}\nreturn [{ json: { text: items[0].text, totalCost: (items[0].usage && items[0].usage.cost) || 0 } }];"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000014",
      "name": "Code: Save community-post-compressed.md",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2500,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $('Code: Create Output Folder').item.json.outputDir;\nconst compressedText = $json.text;\nfs.writeFileSync(`${outputDir}/community-post-compressed.md`, compressedText, 'utf8');\nreturn { json: { compressedText, outputDir, datePrefix: $('Code: Create Output Folder').item.json.datePrefix } };"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000015",
      "name": "Code: Calculate Next Tuesday",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        2750,
        300
      ],
      "parameters": {
        "jsCode": "const datePrefix = $json.datePrefix;\nconst callDate = new Date(datePrefix + 'T12:00:00Z');\nconst dayOfWeek = callDate.getDay();\nconst daysUntilTuesday = (2 - dayOfWeek + 7) % 7 || 7;\nconst nextTuesday = new Date(callDate);\nnextTuesday.setDate(callDate.getDate() + daysUntilTuesday);\n\nconst months = ['January','February','March','April','May','June','July','August','September','October','November','December'];\nconst day = nextTuesday.getUTCDate();\nconst suffix = day === 1 || day === 21 || day === 31 ? 'st' : day === 2 || day === 22 ? 'nd' : day === 3 || day === 23 ? 'rd' : 'th';\nconst formattedDate = `${months[nextTuesday.getUTCMonth()]} ${day}${suffix}`;\nconst inviteDate = `${nextTuesday.getUTCFullYear()}-${String(nextTuesday.getUTCMonth()+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`;\n\nreturn {\n  json: {\n    compressedText: $json.compressedText,\n    outputDir: $json.outputDir,\n    formattedDate,\n    inviteDate,\n    datePrefix\n  }\n};"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000016",
      "name": "Code: Build Invite Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3000,
        300
      ],
      "parameters": {
        "jsCode": "const cfg = $('Code: Pipeline Config').first().json.steps.invite;\nconst compressedText = $input.first().json.compressedText;\nconst formattedDate = $input.first().json.formattedDate;\n\nconst SYSTEM = `You are writing a weekly invite post for an AI community coaching call. Your job is to create a post that encourages members to submit their questions in advance.\n\nYou will be given the compressed community post from the most recent call. Use it to add unique flavor to this week's invite.\n\nThe post MUST follow this exact structure:\n\n1. Opening hook (1-3 sentences) — A playful, attention-grabbing opener that references a specific highlight, tool, insight, or unresolved question from last week's call. Make it intriguing enough that someone who missed last week wants to catch up.\n\n2. Standard section — Copy EXACTLY as written below, including the emoji:\n\n📞 HOW THE CALLS WORK\nThe calls can run 2+ hours.\nWe want to make sure we're respecting everyone's time. Especially those of you who actually show up.\n\nHere's the structure:\n👉 Reply to this post with your questions before the call\n👉 If you submit a question and you're on the call, you go first\n👉 We work through questions in the order they came in\n👉 Then we open it up for everyone else\n\nIf you can't make the call but want your question answered, drop it in the comments. We'll get to it. But priority goes to people who are there.\n\nThe goal is simple: if you're taking the time to show up, you shouldn't have to wait behind questions from people who aren't even on the call.\n\n3. Flavor bridge (1-3 sentences) — Reference 2-3 specific unresolved questions, follow-ups, or topics from last week that people might want to continue. Mention people by first name only if they had open questions. Frame it as a warm invitation, not a call-out.\n\n4. Closing section — Copy EXACTLY as written below, but replace [DATE] with the provided date:\n\n🔗 ZOOM LINK (save this)\nhttps://us06web.zoom.us/j/81995207847?pwd=Xe6u6LmIQOmCP5VTnOwWYjDBfZNKGB.1\n\n📅 WHEN\nTuesday [DATE] at 6PM ET\n\nLooking forward to seeing you on the call!\n\nRules:\n- Tone: playful, welcoming, energetic — like a community leader who genuinely enjoys these calls\n- Do NOT call out anyone negatively or use inside jokes that would confuse non-attendees\n- Do NOT invent information not present in the community post\n- Keep the opening hook and flavor bridge fresh and different each week\n- The standard section and closing section must be reproduced EXACTLY (no modifications to wording or emoji)\n- Total post length should feel like a quick, easy read — not a wall of text`;\n\nreturn [{\n  json: {\n    stepName: 'invite',\n    model: cfg.model,\n    maxTokens: cfg.maxTokens,\n    reasoningEffort: cfg.reasoningEffort,\n    expect: 'none',\n    chunkIndex: 0,\n    system: SYSTEM,\n    user: `Next call date: Tuesday ${formattedDate} at 6PM ET\\n\\nHere is last week's community post:\\n\\n${compressedText}`,\n  },\n}];"
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "openrouterCall",
          "mode": "list",
          "cachedResultName": "OpenRouter Call"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "e1000000-0000-4000-8000-000000000017",
      "name": "Execute: Invite via OpenRouter",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        3125,
        300
      ]
    },
    {
      "id": "ee000000-0000-4000-8000-0000000000e6",
      "name": "Code: Unwrap Invite",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3200,
        300
      ],
      "parameters": {
        "jsCode": "const items = $input.all().map((i) => i.json);\nconst failed = items.filter((i) => !i.ok);\nif (failed.length) {\n  const f = failed[0];\n  throw new Error(`Step ${f.stepName} failed after ${f.attempts} attempts (failureKind=${f.failureKind}). No artifact written.`);\n}\nreturn [{ json: { text: items[0].text, totalCost: (items[0].usage && items[0].usage.cost) || 0 } }];"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000018",
      "name": "Code: Save weekly-invite.md",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3250,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $json.outputDir || $('Code: Create Output Folder').item.json.outputDir;\nconst inviteDate = $('Code: Calculate Next Tuesday').item.json.inviteDate;\nconst inviteText = $json.text;\nfs.writeFileSync(`${outputDir}/${inviteDate}-weekly-invite.md`, inviteText, 'utf8');\nreturn { json: { inviteText, outputDir, inviteDate } };"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000019",
      "name": "HTTP Request: Get Speaker Aliases",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        3500,
        300
      ],
      "parameters": {
        "method": "GET",
        "url": "http://retrieval-server:8999/speaker-aliases-block",
        "options": {
          "response": {
            "response": {
              "responseFormat": "text"
            }
          },
          "timeout": 10000
        }
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000022",
      "name": "Code: Save prepared-transcript.md",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        4000,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $('Code: Create Output Folder').item.json.outputDir;\nconst preparedText = $json.preparedTranscript;\nif (!preparedText) {\n  throw new Error('Prep-prompt produced no content; refusing to write empty prepared-transcript.md');\n}\nfs.writeFileSync(`${outputDir}/prepared-transcript.md`, preparedText, 'utf8');\nreturn { json: { preparedText, outputDir, datePrefix: $('Code: Create Output Folder').item.json.datePrefix } };"
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000023",
      "name": "HTTP Request: POST /ingest",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        4250,
        300
      ],
      "parameters": {
        "method": "POST",
        "url": "http://retrieval-server:8999/ingest",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({\n  session_id: $('Code: Create Output Folder').item.json.datePrefix,\n  session_date: $('Code: Create Output Folder').item.json.datePrefix,\n  session_title: 'Weekly call ' + $('Code: Create Output Folder').item.json.datePrefix,\n  artifact_paths: {\n    prepared_transcript: '/data/output/' + $('Code: Create Output Folder').item.json.datePrefix + '/prepared-transcript.md',\n    extracted_signal:    '/data/output/' + $('Code: Create Output Folder').item.json.datePrefix + '/extracted-signal.md',\n    community_post:      '/data/output/' + $('Code: Create Output Folder').item.json.datePrefix + '/community-post.md'\n  },\n  force_reextract: false\n}) }}",
        "options": {
          "timeout": 1800000,
          "response": {
            "response": {
              "responseFormat": "json"
            }
          },
          "retryOnFailure": {
            "retryOnFailure": true,
            "maxTries": 2,
            "waitBetweenTries": 60000
          }
        }
      },
      "onError": "continueRegularOutput"
    },
    {
      "id": "e1000000-0000-4000-8000-000000000024",
      "name": "Code: Log Ingest Error",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        4500,
        300
      ],
      "parameters": {
        "jsCode": "const fs = require('fs');\nconst outputDir = $('Code: Create Output Folder').item.json.outputDir;\nconst datePrefix = $('Code: Create Output Folder').item.json.datePrefix;\nconst body = $input.first().json;\nconst err = $input.first().error;\nconst ts = new Date().toISOString();\nconst logPath = `${outputDir}/ingest-error.log`;\n\n// Case 1: HTTP-level failure (non-2xx after retry exhaustion, surfaced by\n// onError=continueRegularOutput).\nif (err) {\n  const report = `[${ts}] /ingest failed after retry\\n` +\n                 `status: ${err.httpCode || 'n/a'}\\n` +\n                 `message: ${err.message || JSON.stringify(err)}\\n` +\n                 `response: ${JSON.stringify(err.response || body || {}, null, 2)}\\n`;\n  fs.writeFileSync(logPath, report, 'utf8');\n  return { json: { ingest_status: 'failed', ingest_error: err.message || 'unknown', datePrefix, outputDir } };\n}\n\n// Case 2: HTTP 2xx but the body is not the expected ingest-response shape.\nif (!body || typeof body.chunks_written !== 'number') {\n  const report = `[${ts}] /ingest returned unexpected response shape\\n` +\n                 `body: ${JSON.stringify(body || {}, null, 2)}\\n`;\n  fs.writeFileSync(logPath, report, 'utf8');\n  return { json: { ingest_status: 'failed', ingest_error: 'unexpected response shape', datePrefix, outputDir } };\n}\n\n// Case 3: HTTP 2xx with partial failure. /ingest returns 200 even when\n// chunks_failed > 0 because per-chunk Stage C/B LLM failures do not abort\n// the session. Surface them as a partial-success log so the operator\n// notices and can re-run with force_reextract=true when the upstream\n// issue is resolved.\nif ((body.chunks_failed || 0) > 0) {\n  const warnings = body.warnings || [];\n  const warningsText = warnings.length ? ('\\n  - ' + warnings.join('\\n  - ')) : ' (none)';\n  const report = `[${ts}] /ingest partial failure\\n` +\n                 `chunks_written:  ${body.chunks_written}\\n` +\n                 `chunks_failed:   ${body.chunks_failed}\\n` +\n                 `chunks_skipped:  ${body.chunks_skipped_idempotent}\\n` +\n                 `chunks_by_type:  ${JSON.stringify(body.chunks_by_type || {})}\\n` +\n                 `warnings:${warningsText}\\n\\n` +\n                 `Partial failures typically mean LLM retry exhaustion for\\n` +\n                 `individual chunks. Re-run with force_reextract=true once\\n` +\n                 `the underlying rate-limit or API issue resolves.\\n`;\n  fs.writeFileSync(logPath, report, 'utf8');\n  return { json: { ...body, ingest_status: 'partial', datePrefix, outputDir } };\n}\n\n// Case 4: clean success. Remove any stale ingest-error.log left by a prior\n// failed or partial run so the output directory accurately reflects the\n// current state (empty log would confuse operators; no log == success).\nif (fs.existsSync(logPath)) {\n  fs.unlinkSync(logPath);\n}\nreturn { json: { ...body, ingest_status: 'ok', datePrefix, outputDir } };\n"
      }
    },
    {
      "parameters": {
        "jsCode": "const estimateTokens = (s) => Math.ceil(s.length / 3.6);\n\nfunction splitTranscriptByLines(text, targetTokens) {\n  const lines = text.split('\\n');\n  const chunks = [];\n  let buf = [], bufTokens = 0;\n  for (const line of lines) {\n    const t = estimateTokens(line) + 1;\n    if (bufTokens + t > targetTokens && buf.length > 0) {\n      chunks.push(buf.join('\\n'));\n      buf = []; bufTokens = 0;\n    }\n    buf.push(line); bufTokens += t;\n  }\n  if (buf.length && buf.join('\\n').trim()) chunks.push(buf.join('\\n'));\n  return chunks;\n}\n\nconst CANON = ['general', 'insights', 'qa', 'tools', 'links', 'decisions'];\n\nfunction splitSignalIntoSections(md) {\n  const out = {};\n  const re = /^#{1,3}[ \\t]+([a-z]+)[ \\t]*$/gm;\n  let m; const marks = [];\n  while ((m = re.exec(md)) !== null) marks.push({ slug: m[1], start: m.index, bodyStart: re.lastIndex });\n  for (let i = 0; i < marks.length; i++) {\n    const end = i + 1 < marks.length ? marks[i + 1].start : md.length;\n    const body = md.slice(marks[i].bodyStart, end).trim();\n    if (CANON.includes(marks[i].slug) && body) out[marks[i].slug] = body;\n  }\n  return out;\n}\n\nfunction reassemblePrep(texts) {\n  const SESSION_RE = /===\\s*SESSION\\s*===[\\s\\S]*?(?=\\n<!--SEGMENT|\\n===|$)/;\n  const UNRES_RE = /===\\s*UNRESOLVED SPEAKERS\\s*===([\\s\\S]*)$/;\n  let header = null;\n  const bodies = [], unresolved = new Set();\n  for (const raw of texts) {\n    let t = raw;\n    const u = t.match(UNRES_RE);\n    if (u) {\n      u[1].split('\\n').map((l) => l.trim()).filter((l) => l.startsWith('-')).map((l) => l.replace(/\\s+/g, ' ')).forEach((l) => unresolved.add(l));\n      t = t.slice(0, u.index);\n    }\n    const segIdx = t.indexOf('<!--SEGMENT');\n    if (segIdx > 0) {\n      const head = t.slice(0, segIdx);\n      const m = head.match(/===\\s*SESSION\\s*===[\\s\\S]*$/);\n      if (m) {\n        if (header === null) header = m[0].trim();\n        t = t.slice(segIdx);\n      }\n    }\n    if (t.trim()) bodies.push(t.trim());\n  }\n  let out = (header ? header + '\\n\\n' : '') + bodies.join('\\n\\n');\n  if (unresolved.size) out += '\\n\\n=== UNRESOLVED SPEAKERS ===\\n' + [...unresolved].join('\\n');\n  return out;\n}\n"
      },
      "id": "c11b0000-0000-4000-8000-00000000c11b",
      "name": "Code: Chunk Lib",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        220,
        600
      ],
      "disabled": true
    },
    {
      "parameters": {
        "jsCode": "return [{\n  json: {\n    steps: {\n      prep:         { model: 'z-ai/glm-5.3-flash',        maxTokens: 32768, reasoningEffort: 'low', chunkTargetTokens: 15000 },\n      signalMap:    { model: 'anthropic/claude-sonnet-5', maxTokens: 16384, chunkTargetTokens: 15000 },\n      signalReduce: { model: 'anthropic/claude-sonnet-5', maxTokens: 32768, budgetTokens: 8000 },\n      postSection:  { model: 'z-ai/glm-5.3-flash',        maxTokens: 16384, reasoningEffort: 'low' },\n      compress:     { model: 'z-ai/glm-5.3-flash',        maxTokens: 16384, reasoningEffort: 'low' },\n      invite:       { model: 'z-ai/glm-5.3-flash',        maxTokens: 16384, reasoningEffort: 'low' },\n    },\n    retry: { componentAttempts: 3, callerHalvings: 2 },\n  },\n}];"
      },
      "id": "cf000000-0000-4000-8000-0000000000cf",
      "name": "Code: Pipeline Config",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        120,
        0
      ]
    },
    {
      "id": "ee000000-0000-4000-8000-0000000000e2",
      "name": "Code: Split Prep",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3620,
        300
      ],
      "parameters": {
        "jsCode": "const cfg = $('Code: Pipeline Config').first().json.steps.prep;\nconst src = $('Code: Validate and Check Partner').first().json.transcriptText;\nconst halving = $input.first().json.halving || 0;\nconst target = Math.floor(cfg.chunkTargetTokens / Math.pow(2, halving));\n\nconst estimateTokens = (s) => Math.ceil(s.length / 3.6);\n\nfunction splitTranscriptByLines(text, targetTokens) {\n  const lines = text.split('\\n');\n  const chunks = [];\n  let buf = [], bufTokens = 0;\n  for (const line of lines) {\n    const t = estimateTokens(line) + 1;\n    if (bufTokens + t > targetTokens && buf.length > 0) {\n      chunks.push(buf.join('\\n'));\n      buf = []; bufTokens = 0;\n    }\n    buf.push(line); bufTokens += t;\n  }\n  if (buf.length && buf.join('\\n').trim()) chunks.push(buf.join('\\n'));\n  return chunks;\n}\n\nconst aliases = $('HTTP Request: Get Speaker Aliases').first().json.data || '';\nconst SYSTEM = `You are preparing a meeting transcript for embedding in a semantic knowledge base. Transform the raw transcript into an enriched, topic-segmented document that will produce high-quality, independently retrievable chunks when split.\n\n## Speaker Name Normalization\n\nApply the speaker alias map supplied in the \\`SPEAKER_ALIASES\\` context block below. If you encounter a speaker whose raw name is NOT in that map, do NOT guess a canonical form — pass the name through unchanged AND list it under \"=== UNRESOLVED SPEAKERS ===\" at the end of your output.\n\n${aliases}\n\n## Pass 1 — Clean the transcript\n\n- Apply speaker normalization above\n- Fix obvious transcription artifacts: \"gpt four\" → \"GPT-4\", \"llm\" → \"LLM\", \"open a i\" → \"OpenAI\"\n- Remove pure filler (stutters, \"um\", \"uh\") only when they add no meaning\n- Preserve all timestamps and speaker attribution exactly\n\n## Pass 2 — Segment by topic\n\nDivide the transcript into coherent topic blocks. Start a new segment when the conversation meaningfully shifts subject. For each segment, prepend a structured header:\n\n<!--SEGMENT\ntopic: <2–5 word label>\nspeakers: <comma-separated list of speakers who contribute>\nkeywords: <8–12 terms: tools, models, concepts, companies, people explicitly mentioned>\nsummary: <2–3 sentence description of what this segment covers and why it matters>\n-->\n\nTarget 300–500 words per segment body. If a topic recurs later, open a new segment — do not merge non-contiguous discussion.\n\n## Pass 3 — Inline annotations\n\nWithin each segment body, mark key moments:\n\n- When a tool, service, model, or product is named: append \\`[tool:name]\\` after first mention\n- When someone asks a question: wrap with \\`<Q>\\` ... \\`</Q>\\`\n- When someone answers it: wrap with \\`<A>\\` ... \\`</A>\\`\n- When a URL or resource is shared: append \\`[link:url-or-description]\\`\n- When a sentence contains a concrete recommendation or takeaway: prefix with ▶\n\n## Output format\n\nThis is ONE PART of a longer transcript. Emit segments for the portion you are given. Include a \\`=== SESSION ===\\` header block (date, duration_estimate, main_themes) and, if any speakers were not in the alias map, a trailing \\`=== UNRESOLVED SPEAKERS ===\\` block. Both are merged across parts downstream.\n\n## Quality constraints\n\n- Do not invent or infer content not present in the source\n- Every segment must be independently understandable\n- Segment header \\`keywords\\` and \\`summary\\` are what get embedded — make them precise and information-dense\n- Prefer 8 focused segments over 4 sprawling ones`;\n\nreturn splitTranscriptByLines(src, target).map((chunk, i) => ({\n  json: {\n    stepName: 'prep',\n    model: cfg.model,\n    maxTokens: cfg.maxTokens,\n    reasoningEffort: cfg.reasoningEffort,\n    expect: 'prep.chunk',\n    chunkIndex: i,\n    system: SYSTEM,\n    user: chunk,\n  },\n}));"
      }
    },
    {
      "parameters": {
        "workflowId": {
          "__rl": true,
          "value": "openrouterCall",
          "mode": "list",
          "cachedResultName": "OpenRouter Call"
        },
        "options": {
          "waitForSubWorkflow": true
        }
      },
      "id": "ee000000-0000-4000-8000-0000000000e1",
      "name": "Execute: Prep via OpenRouter",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.2,
      "position": [
        3750,
        300
      ]
    },
    {
      "id": "ee000000-0000-4000-8000-0000000000e3",
      "name": "Code: Aggregate Prep",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        3880,
        300
      ],
      "parameters": {
        "jsCode": "const items = $input.all().map((i) => i.json);\nconst failed = items.filter((i) => !i.ok);\nif (failed.length) {\n  const f = failed[0];\n  throw new Error(\n    `Prep step failed on chunk ${f.chunkIndex} after ${f.attempts} attempts ` +\n    `(failureKind=${f.failureKind}, finishReason=${f.finishReason}). No artifact written.`\n  );\n}\n\nfunction reassemblePrep(texts) {\n  const SESSION_RE = /===\\s*SESSION\\s*===[\\s\\S]*?(?=\\n<!--SEGMENT|\\n===|$)/;\n  const UNRES_RE = /===\\s*UNRESOLVED SPEAKERS\\s*===([\\s\\S]*)$/;\n  let header = null;\n  const bodies = [], unresolved = new Set();\n  for (const raw of texts) {\n    let t = raw;\n    const u = t.match(UNRES_RE);\n    if (u) {\n      u[1].split('\\n').map((l) => l.trim()).filter((l) => l.startsWith('-')).map((l) => l.replace(/\\s+/g, ' ')).forEach((l) => unresolved.add(l));\n      t = t.slice(0, u.index);\n    }\n    const segIdx = t.indexOf('<!--SEGMENT');\n    if (segIdx > 0) {\n      const head = t.slice(0, segIdx);\n      const m = head.match(/===\\s*SESSION\\s*===[\\s\\S]*$/);\n      if (m) {\n        if (header === null) header = m[0].trim();\n        t = t.slice(segIdx);\n      }\n    }\n    if (t.trim()) bodies.push(t.trim());\n  }\n  let out = (header ? header + '\\n\\n' : '') + bodies.join('\\n\\n');\n  if (unresolved.size) out += '\\n\\n=== UNRESOLVED SPEAKERS ===\\n' + [...unresolved].join('\\n');\n  return out;\n}\n\nconst ordered = items.slice().sort((a, b) => a.chunkIndex - b.chunkIndex);\nreturn [{\n  json: {\n    preparedTranscript: reassemblePrep(ordered.map((i) => i.text)),\n    chunkCount: ordered.length,\n    totalCost: ordered.reduce((s, i) => s + ((i.usage && i.usage.cost) || 0), 0),\n  },\n}];"
      }
    }
  ],
  "connections": {
    "Manual Trigger": {
      "main": [
        [
          {
            "node": "Code: Pipeline Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Validate and Check Partner": {
      "main": [
        [
          {
            "node": "Code: Merge Content",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Merge Content": {
      "main": [
        [
          {
            "node": "Code: Create Output Folder",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Create Output Folder": {
      "main": [
        [
          {
            "node": "Code: Save transcript.txt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Save transcript.txt": {
      "main": [
        [
          {
            "node": "Code: Split Signal",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Save extracted-signal.md": {
      "main": [
        [
          {
            "node": "Code: Split Post Sections",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Split Post Sections": {
      "main": [
        [
          {
            "node": "Execute: Post Sections via OpenRouter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute: Post Sections via OpenRouter": {
      "main": [
        [
          {
            "node": "Code: Assemble Post",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Assemble Post": {
      "main": [
        [
          {
            "node": "Code: Save community-post.md",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Save community-post.md": {
      "main": [
        [
          {
            "node": "Code: Build Compress Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Build Compress Request": {
      "main": [
        [
          {
            "node": "Execute: Compress via OpenRouter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute: Compress via OpenRouter": {
      "main": [
        [
          {
            "node": "Code: Unwrap Compress",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Unwrap Compress": {
      "main": [
        [
          {
            "node": "Code: Save community-post-compressed.md",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Save community-post-compressed.md": {
      "main": [
        [
          {
            "node": "Code: Calculate Next Tuesday",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Calculate Next Tuesday": {
      "main": [
        [
          {
            "node": "Code: Build Invite Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Build Invite Request": {
      "main": [
        [
          {
            "node": "Execute: Invite via OpenRouter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute: Invite via OpenRouter": {
      "main": [
        [
          {
            "node": "Code: Unwrap Invite",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Unwrap Invite": {
      "main": [
        [
          {
            "node": "Code: Save weekly-invite.md",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Save weekly-invite.md": {
      "main": [
        [
          {
            "node": "HTTP Request: Get Speaker Aliases",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP Request: Get Speaker Aliases": {
      "main": [
        [
          {
            "node": "Code: Split Prep",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Save prepared-transcript.md": {
      "main": [
        [
          {
            "node": "HTTP Request: POST /ingest",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP Request: POST /ingest": {
      "main": [
        [
          {
            "node": "Code: Log Ingest Error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Pipeline Config": {
      "main": [
        [
          {
            "node": "Code: Validate and Check Partner",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Split Prep": {
      "main": [
        [
          {
            "node": "Execute: Prep via OpenRouter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute: Prep via OpenRouter": {
      "main": [
        [
          {
            "node": "Code: Aggregate Prep",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Aggregate Prep": {
      "main": [
        [
          {
            "node": "Code: Save prepared-transcript.md",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Split Signal": {
      "main": [
        [
          {
            "node": "Execute: Signal Map via OpenRouter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute: Signal Map via OpenRouter": {
      "main": [
        [
          {
            "node": "Code: Build Signal Reduce",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Build Signal Reduce": {
      "main": [
        [
          {
            "node": "Execute: Signal Reduce via OpenRouter",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Execute: Signal Reduce via OpenRouter": {
      "main": [
        [
          {
            "node": "Code: Aggregate Signal",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code: Aggregate Signal": {
      "main": [
        [
          {
            "node": "Code: Save extracted-signal.md",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {},
  "tags": []
}
