# Disposable VM108 validation only. Internal network, no live credentials or data.
name: cbm-stabilization-dev
services:
  postgres:
    image: postgres:18.6
    environment:
      POSTGRES_USER: fixture
      POSTGRES_DB: cbm_stabilization
      POSTGRES_HOST_AUTH_METHOD: trust
    tmpfs:
      - /var/lib/postgresql
    mem_limit: 512m
    healthcheck:
      test: [CMD-SHELL, "pg_isready -U fixture -d cbm_stabilization"]
      interval: 2s
      timeout: 2s
      retries: 30
    networks:
      default:
        aliases: [cbm-auto-pg]
  nats:
    image: nats:2.10.27
    command: [-js, -sd, /data]
    tmpfs: [/data]
    mem_limit: 128m
    networks:
      default:
        aliases: [cbm-auto-nats]
  check:
    image: community-brain:stabilization-20260920
    read_only: true
    user: "10001:10001"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    mem_limit: 1200m
    cpus: 1.5
    tmpfs:
      - /tmp:uid=10001,gid=10001
      - /state:uid=10001,gid=10001,mode=0700
    volumes:
      - ../../..:/workspace:ro
    environment:
      CB_DATABASE_URL: postgresql+psycopg://fixture@cbm-auto-pg/cbm_stabilization
      CB_STORAGE_ROOT: /state/files
      CB_PIPELINE_CONFIG_DIR: /state/config
      CB_CORPUS_ROOT: /state/corpus
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPATH: /workspace/deploy/community-brain/automatic:/workspace/deploy/community-brain/automatic-rehearsal
    depends_on:
      postgres:
        condition: service_healthy
      nats:
        condition: service_started
    command: [python, /workspace/deploy/community-brain/stabilization/check.py]
networks:
  default:
    internal: true
