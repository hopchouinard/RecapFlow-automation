# Request033 actual production mappings

This immutable candidate maps the installed management source, actual database schema, service ordering and private preservation contracts. Production execution remains disabled. Read MAPPING-LEDGER.md for effects and uncertainty handling and OPERATOR-PROPOSAL.md for the bounded production proposal.

The isolated implementation executes actual installed helper algorithms and file writers with named transport fixtures, actual PostgreSQL18.6 with the deployed schema and synthetic rows, actual Docker generation replacement, and actual systemd interruption/recovery. Every transport double is identified in its receipt. Neither shape checks nor synthetic data qualify private production restoration or live sessions.

Run `python3 -B -m unittest discover -s . -p 'test_*.py'`, the five Forge tests under forge, and the retained recovery-core suite. `verify_mapping.py` checks byte lineage. VM drivers require the exclusive Request033 development root and recorded fixture IDs. Sealed directories reject changed/extra/bytecode members. Do not edit sealed packets, replay an unresolved operation, or reuse retained fixture names. No production installer or enable switch is supplied.
