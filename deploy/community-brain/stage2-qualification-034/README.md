# Request034 stage 2 development qualification

This tree is the home.servers response source for `CBM-STAGE2-QUALIFICATION-20260923-034`. It derives the isolated VM108 development runner from `management-successor-028` and the exact Request033 mapping contract. `dev-runtime/ORIGIN.json` records the pre-relink ancestry; each staged `packet-vN` has its own immutable manifest and archive hash in the private Mac state. The final VM108 source packet is identified in the acceptance ledger. No packet has been installed on VM109 or VM101, and the parent repository's `agent-ops` gitlink has not been advanced.

The dev runner uses an actual Infisical project path `/development/community-brain-dev/request034` (within the existing `homelab` project), a newly issued Step CA leaf for the dev NATS listener, pinned API/WebUI images, synthetic PostgreSQL/corpus/provider content, and live dev API, WebUI filter, collector configuration, Kuma and Prometheus consumers. It sends no paid provider request. The dev fixture is under `/srv/dev-data/workspaces/cbm-stage2-qualification-20260923-034` on VM108; private credentials, database and WebUI volumes remain there or in `~/.local/state/community-brain-management/request034`, not in this source tree or the relay.

`production_admission.py` is a fail-closed *review core*. It checks external plan identity, current authority/expiry, exact VM109/mount/incumbent/hold/lock bindings, and the exact 21 production evidence names. It always returns production execution disabled. It is not a controller or installer, does not verify a live production evidence receipt's contents, and does not provide an independent finalizer. The Request033 production compiler/controller refusals are unchanged. The production execution path is **not reviewable for execution** until those mechanisms, all production evidence, and their VM108 fault tests exist and pass. Development receipts cannot satisfy production slots.

The matching test is `python3 -B -m unittest discover -s platform-services/community-brain-prod/stage2-qualification-034 -p 'test_*.py'`. The copied admission core and six negative tests also ran from the sealed final packet on VM108. The dev runtime's nine source-boundary tests are separate. See `ACCEPTANCE-LEDGER.md` and `BROWSER-OBSERVATION.md` for outcomes and limits.
