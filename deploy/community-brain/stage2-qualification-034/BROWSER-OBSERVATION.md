# Request034 rendered browser acceptance

At 2026-09-23T23:16–23:26Z, Chrome rendered the isolated VM108 WebUI through a local SSH tunnel. A synthetic development account signed in through the visible login form. The exact-image WebUI showed the user prompt "Request034 synthetic recovery chat. Respond with the synthetic marker." and the assistant response "Request034 synthetic model response. No paid provider or production data." A 60-byte synthetic file, `browser-upload.txt` (SHA-256 `e9df85ca2c5f8cb73f8b8c3e117cd23c63618ab6910e390d573c40ecfe9c75e2`), was selected through Chrome's native file picker and sent with a second prompt. The visible response displayed one retrieved source. After a page reload and after a separate WebUI container recreation, the same chat, both assistant responses, the file name and size, and the source indicator remained visible.

The persisted dev WebUI environment was changed to `ENABLE_SIGNUP=false`. Its `/api/v1/auths/signup` endpoint returned HTTP 403, while the existing synthetic account still signed in. After logging out in Chrome, the rendered `/auth` page contained only email, password, visibility, and login controls; no signup control appeared. This was a development account and synthetic file, with no real user content or paid provider call.

The first chat attempt before the synthetic provider acquired its completion endpoint retained only the user message. The successful new chat followed a separately sealed provider source revision. The first signup-off verification timed out during the exact image's cold start; the independent post-start readback passed. Both attempts are retained in the private dev workspace and are not represented as initial-pass successes.
